# MCP资源和提示-超出工具的上下文暴露

> 工具获得了90%的MCP关注。另外两个服务器原语解决不同的问题。资源公开数据供读取；提示符将可重用模板作为斜杠命令公开。许多服务器应该使用资源而不是在工具中包装读取，使用提示而不是在客户端提示中硬编码工作流。本课命名决策规则并将其付诸实践

**Type:** Build
**Languages:** Python (stdlib, resource + prompt handler)
**Prerequisites:** Phase 13 · 07 (MCP server)
**Time:** ~45 minutes

## 学习目标

- 决定是将功能公开为工具、资源还是给定域的提示符。
- 实施
- 实施
- 识别主机显示的提示是斜杠命令还是自动注入上下文。

## 这个问题

一个用于笔记应用程序的朴素MCP服务器将所有东西都暴露为工具：这将每个数据访问封装在一个模型驱动的工具调用中。后果:

- 模型必须决定是否调用
- 只读内容不能订阅或流到主机的侧板。
- 客户端ui （Claude Desktop的资源附件面板，Cursor的“包括文件”选择器）不能显示数据。

正确的分割：将数据作为资源公开，将变化或计算的操作作为工具公开，将可重用的多步骤工作流作为提示公开。每个原语都有自己的用户体验功能和访问模式。

## 这个概念

### 工具vs资源vs提示——决策规则

| Capability | Primitive |
|------------|-----------|
| User wants to search, filter, or transform data | tool |
| User wants the host to include this data as context | resource |
| User wants a templated workflow they can re-run | prompt |

指导原则：如果在每个相关查询中调用模型都能使模型受益，那么它就是一个工具。如果用户将其附加到对话中受益，则它是一种资源。如果整个多步骤工作流是用户想要重用的单元，则它是提示符。

### 资源

Z0`resources/read` takes `{uri}` and returns `{contents: [{uri, mimeType, text | blob}]}`.

uri可以是任何可寻址的：

- Z0
- Z0
- Z0
- Z0

Z0二进制用法

### 资源订阅

申报客户端呼叫服务器发送客户端重新读取。

用例：一个笔记服务器，其资源是磁盘上的文件；文件监视器触发更新通知；Claude Desktop在主机外编辑时将文件重新拉入上下文。

### 资源模板（2025-11-25新增）

Z0客户端可以在资源选择器中自动完成id。

### 提示

Z0`prompts/get` takes `{name, arguments}` and returns `{description, messages: [{role, content}]}`.

提示符是一个模板，填充到主机向其模型提供的消息列表中。例如，a

### 主机和提示符

Claude Desktop、VS Code和Cursor在聊天界面中将提示作为斜杠命令。用户类型服务器的提示符是“用户快捷方式”和“发送给模型的完整提示符”之间的契约。

不是每个客户端都支持提示-检查能力协商。声明了提示功能的服务器，但没有提示支持的客户端根本看不到斜杠命令。

### “列表更改”通知

资源和提示符都会发出一个刚刚导入了20个新票据的票据服务器发出客户回电

### 内容类型约定

文本：
对于二进制：
对于MCP应用程序（第14课）：

### 动态资源

资源URI不必对应于静态文件。`notes://recent` can return the latest five notes on every read. `db://query/users/active` can execute a parameterized query. 服务器可以自由地动态计算内容。

规则：如果客户端可以通过URI缓存，那么URI必须是稳定的。如果计算是一次性的，那么URI应该包含时间戳或nonce，这样客户端缓存就不会过期。

### 订阅与轮询

支持订阅的客户端通过服务器推送不支持订阅的预订阅客户端或主机通过重新读取轮询。两者都符合规范。服务器的功能声明告诉客户机它支持哪些功能。

订阅成本：服务器上的每个会话状态（谁订阅了什么）。保持订阅集有界；断开连接的客户端应该超时。

### 提示与系统提示

MCP中的提示不是系统提示。主机的系统提示符（它自己的操作指令）和MCP提示符（由用户调用的服务器提供的模板）并行运行。一个行为良好的客户端永远不会让服务器提示覆盖它自己的系统提示；它使它们分层。

## 使用它

Z0

- 每音符资源(
- A
- 一个文件监视程序模拟，它发出
- A

运行演示以查看完整流程。

## 船这

这一课产生了给定一个建议的MCP服务器，该技能将每个功能分类为工具/资源/提示。

## 练习

1. 运行观察初始资源列表，然后触发注释编辑并验证

2. 添加一个

3. 为GitHub MCP服务器设计三个提示符：每个都有参数模式。提示体应该是可运行的，无需进一步编辑。

4. 以第07课服务器中的一个现有工具为例，并对它进行分类，看它是应该保留为一个工具，还是应该分割为一个资源加工具对。用一句话证明。

5. 阅读说明书识别中的一个字段提示：看看

## 关键术语

| Term | What people say | What it actually means |
|------|----------------|------------------------|
| Resource | "Exposed data" | URI-addressable content the host can read |
| Resource URI | "Pointer to data" | Scheme-prefixed identifier (`file://`, `notes://`, etc.) |
| `resources/subscribe` | "Watch for changes" | Client-opt-in server-push updates for a specific URI |
| `notifications/resources/updated` | "Resource changed" | Signal to client that a subscribed resource has new content |
| Resource template | "Parameterized URI" | URI pattern with completion hints for the host picker |
| Prompt | "Slash-command template" | Named multi-message template with argument slots |
| Prompt arguments | "Template inputs" | Typed parameters the host collects before rendering |
| `prompts/get` | "Render template" | Server returns the filled-in message list |
| Content block | "Typed chunk" | `{type: text | image | resource | ui_resource}` |
| Slash-command UX | "User shortcut" | Host surfaces prompts as commands starting with `/` |

## 进一步的阅读

- Z0
- Z0
- Z1
- Z1
- Z0