# 问答系统

> 三个系统塑造了现代QA。提取发现跨度。检索增强将它们建立在文档中。生成生成的答案。每一个现代人工智能助手都是这三者的混合体。

**Type:** Build
**Languages:** Python
**Prerequisites:** Phase 5 · 11 (Machine Translation), Phase 5 · 10 (Attention Mechanism)
**Time:** ~75 minutes

## 这个问题

用户输入“第一代iPhone何时发布？”，得到的答案是“2007年6月29日”。而不是“苹果的历史悠久而多变。”不是“2007”被单独监禁却没有判刑。一个直接的，有根据的，正确的答案。

在过去十年中，有三种架构主导着QA。

- **采掘QA。**给定一个问题和一篇已知包含答案的文章，找出文章中答案跨度的开始和结束索引。SQuAD是典型的基准。
- ** Open-domain QA。**文章没有给出。首先检索相关段落，然后提取或生成答案。这是今天每个RAG管道的基石。
- **生成式/闭式QA。**大型语言模型从其参数存储器中进行应答。没有检索。推理最快，对事实最不可靠。

2026年的趋势是混合的：检索最好的几个段落，然后提示生成模型以这些段落为基础进行回答。这就是RAG，第14课深入介绍了检索的一半内容。这一课构建了QA的一半。

## 这个概念


**萃取。**将问题和段落与转换器（BERT族）一起编码。训练两个预测答案的开始和结束标记指标的头。损失是有效位置上的交叉熵。输出是通道的一个跨度。永远不要产生幻觉，永远不要处理文章无法回答的问题。

** Retrieval-augmented(布)。**两个阶段。首先，一只寻回犬找到了顶部其次，读者（抽取或生成）利用这些段落得出答案。寻回者与读者的分离让每个人都能独立地接受训练和评估。现代RAG通常在它们之间添加一个重排序符。

**生成。**只有解码器的LLM （GPT, Claude, Llama）从学习权重中得到答案。没有检索步骤。在常识方面非常出色，在罕见或最近的事实方面则是灾难性的。在预训练数据中，幻觉率与事实频率呈负相关。

## 构建它

### 步骤1：使用预训练模型提取QA

”“python
从Transformer进口管道

Qa = pipeline（"question-answer ", model="deepset/roberta-base-squad "）

通道= (
“苹果公司于2007年6月29日发布了第一款iPhone。”
2007年1月，史蒂夫·乔布斯在Macworld大会上宣布了这款设备。”
）
第一代iPhone是什么时候发布的？

Answer = qa（question=question, context=passage）
打印(答案)
```

```python
{'score': 0.98, 'start': 57, 'end': 70, 'answer': 'June 29, 2007'}
```

Z0默认情况下，得到明确的“不回答”行为，通过一定要检查

### 步骤2：检索增强的管道（草图）

”“python
从sentence_transformer导入SentenceTransformer
import numpy as np

encoder = SentenceTransformer（"sentence-transformer /all-MiniLM-L6-v2"）

语料= [
苹果公司于2007年6月29日发布了第一款iPhone。
“2007年的Macworld上，史蒂夫·乔布斯发布了iPhone。”
Android于2008年作为bb0的移动操作系统推出。
第一款iPod是在2001年发布的。
］
Corpus_embeddings =编码器。编码(语料库,normalize_embeddings = True)


Def retrieve(question, top_k=2)：
Q_emb =编码器。编码([问题],normalize_embeddings = True)
sims = (corpus_embeddings @ q_emb.T).squeeze（）
Order = np.argsort(-sims)[:top_k]
按顺序返回[corpus[i] for i]


def回答(提问):
（题目，top_k=2）
Combined = " ".join（段落）
返回qa（question=question, context=combined）


回答（“第一代iPhone是什么时候发布的？”））
```

Two-stage pipeline. Dense retriever (Sentence-BERT) finds relevant passages by semantic similarity. Extractive reader (RoBERTa-SQuAD) pulls the answer span from the combined top passages. Works on small corpora. For a million-document corpus, use FAISS or a vector database.

### Step 3: generative with RAG

```python
def rag_generate(question, llm):
    passages = retrieve(question, top_k=3)
    prompt = f"""Context:
{chr(10).join('- ' + p for p in passages)}

Question: {question}

Answer using only the context above. If the context does not contain the answer, say "I don't know."
"""
    return llm(prompt)
```

提示模式很重要。与单纯提示相比，明确地告诉模型在上下文中扎根并在上下文不充分时返回“我不知道”，可以使幻觉率降低40-60%。更精细的模式添加了引用、置信度分数和结构化提取。

### 步骤4：反映真实世界的评估

SQuAD使用**精确匹配(EM)**和**令牌级F1**。EM是经过规范化后的严格匹配（小写字母、去掉标点符号、删除条目）——要么预测完全匹配，要么得分为0。F1是在预测和参考之间的标记重叠上计算的，并给出部分信用。两个信用不足的解释：“2007年6月29日”和“2007年6月29日”通常得到0 EM（顺序打破常态），但仍然从重叠的令牌中获得大量F1。

生产QA：

- **答案准确性**（LLM判断或人工判断，因为指标不捕获语义等价）。
- **引用准确性。**引用的文章真的支持答案吗？自动检查生成的引用和检索的段落之间的字符串匹配是微不足道的。
- **拒绝校准。**当答案不在检索的文章中，系统是否正确地说“我不知道”？测量假置信率。
- **检索召回。**在评估读卡器之前，测量一下猎犬是否得到了进入顶部的正确通道读者无法弥补缺失的段落。

### RAGAS: 2026年生产评估框架

Z0它在四个维度上得分，而不需要黄金参考：

- **诚实。**答案中的每个声明是否都来自检索到的上下文？以nli为基础的蕴涵来衡量。你的主要幻觉指标。
- **回答相关性。答案是否针对问题？通过从答案中产生假设性问题并与真实问题进行比较来衡量。
- **上下文精度。**在检索到的数据块中，实际相关的比例是多少？低精度=提示噪声。
- **上下文召回。**检索集是否包含所有需要的信息？低召回=读取无法成功。

无参考评分让您评估现场生产流量没有策划黄金答案。对于那些完全匹配的指标毫无用处的开放式问题，将llm作为裁判放在最上面。

Z0插上你的寻回器+阅读器。每个查询获得四个标量。警惕回归。

## 使用它

2026年的堆栈。

| Use case | Recommended |
|---------|-------------|
| Given passage, find answer span | `deepset/roberta-base-squad2` |
| Over a fixed corpus, closed-book not acceptable | RAG: dense retriever + LLM reader |
| Real-time over a document store | RAG with hybrid (BM25 + dense) retriever + reranker (lesson 14) |
| Conversational QA (follow-up questions) | LLM with conversation history + RAG on each turn |
| Highly factual, regulated domains | Extractive over an authoritative corpus; never generative alone |

抽取QA在2026年是不流行的，因为拥有LLM的RAG处理更多的案例。它仍然在需要字面引用的环境中发布：法律研究，法规遵从性，审计工具。

## 船这

另存为

”“减价
---
名称:qa-architect
描述：选择QA架构、检索策略和评估计划。
版本:1.0.0
阶段:5
教训:13
标签：[nlp， qa, rag]
---

给定需求（语料库大小、问题类型、事实约束、延迟预算），输出：

1. 体系结构。提取，带提取阅读器的RAG，带生成阅读器的RAG，或者闭卷LLM。一句话的原因。
2. 寻回犬。无，BM25，密集（命名编码器），或混合。
3. 读者。小组调优模型，名称为LLM，或“领域调优蒸馏酒”。
4. 评估。EM + F1为采掘基准；回答准确性+引用准确性+拒绝校准生产。说出你在测量什么以及你是如何测量它的。

对于监管或合规敏感问题，拒绝闭卷式LLM答案。拒绝任何没有检索-召回基线的QA系统（在不知道检索器找到了正确的段落之前，您无法评估阅读器）。将需要多跳推理的问题标记为需要专门的多跳检索器（如hotpotqa训练的系统）。
```

## Exercises

1. **Easy.** Set up the SQuAD extractive pipeline above on 10 Wikipedia passages. Hand-craft 10 questions. Measure how often the answer is correct. You should see 7-9 correct if passages and questions are clean.
2. **Medium.** Add a refusal classifier. When the top retrieval score is below a threshold (say 0.3 cosine), return "I don't know" instead of calling the reader. Tune the threshold on a held-out set.
3. **Hard.** Build a RAG pipeline over a 10,000-document corpus of your choice. Implement hybrid retrieval (BM25 + dense) with RRF fusion (see lesson 14). Measure answer accuracy with and without the hybrid step. Document which question types benefit most.

## Key Terms

| Term | What people say | What it actually means |
|------|-----------------|-----------------------|
| Extractive QA | Find the answer span | Predict start and end indices of the answer within a given passage. |
| Open-domain QA | QA over a corpus | No given passage; must retrieve then answer. |
| RAG | Retrieve then generate | Retrieval-augmented generation. Retriever + reader pipeline. |
| SQuAD | Canonical benchmark | Stanford Question Answering Dataset. EM + F1 metrics. |
| Hallucination | Made-up answer | Reader output not supported by retrieved context. |
| Refusal calibration | Know when to shut up | System correctly says "I don't know" when unable to answer. |

## Further Reading

- [Rajpurkar et al. (2016). SQuAD: 100,000+ Questions for Machine Comprehension of Text](https://arxiv.org/abs/1606.05250) — the benchmark paper.
- [Karpukhin et al. (2020). Dense Passage Retrieval for Open-Domain QA](https://arxiv.org/abs/2004.04906) — DPR, the canonical dense retriever for QA.
- [Lewis et al. (2020). Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks](https://arxiv.org/abs/2005.11401) — the paper that named RAG.
- [Gao et al. (2023). Retrieval-Augmented Generation for Large Language Models: A Survey](https://arxiv.org/abs/2312.10997) — comprehensive RAG survey.