# 注意变量-滑动窗口，稀疏，微分

> 全神贯注是一个圆圈。每个标记都能看到每个标记，内存为此付出代价。四种不同的弯曲形状的圆和回收一半的成本。

**Type:** Build
**Languages:** Python
**Prerequisites:** Phase 7 · 02 (Self-Attention), Phase 7 · 03 (Multi-Head), Phase 7 · 12 (KV Cache / Flash Attention)
**Time:** ~60 minutes

## 这个问题

全部注意力成本对于128k上下文的Llama 370b，每层有160亿个注意条目，乘以80层。短暂的注意力（第十二课）隐藏了

有三类变体改变了注意力矩阵本身的拓扑结构：

1. **滑动窗口注意（SWA）。**每个令牌关注一个固定的邻居窗口，而不是完整的前缀。内存和计算下降到杰玛2/3，Mistral 7B的第一层，Phi-3-Long。
2. **稀疏/阻塞注意力。**仅选对其余的则被迫减重为零。Longformer, BigBird， OpenAI稀疏变压器。
3. * *微分的注意。**计算两个具有独立Q/K投影的注意图，从另一个中减去一个。消除“注意力下沉”，这种下沉会让前几个代币失去分量。微软的DIFF变压器（2024）。

这些共存。2026前沿模型通常将它们混合在一起：大多数层是SWA-1024，每五分之一是全局全注意力，少数是清理检索的差分头。Gemma 3的swa / global比率为5:1，这是目前教科书上的默认值。

## 这个概念

### 滑动窗口注意（SWA）

每个位置的查询窗外的令牌得到

```
full causal:           sliding window (W=4):
positions 0-7          positions 0-7, W=4
    0 1 2 3 4 5 6 7        0 1 2 3 4 5 6 7
0 | x                0 |  x
1 | x x              1 |  x x
2 | x x x            2 |  x x x
3 | x x x x          3 |  x x x x
4 | x x x x x        4 |    x x x x
5 | x x x x x x      5 |      x x x x
6 | x x x x x x x    6 |        x x x x
7 | x x x x x x x x  7 |          x x x x
```

因为

*KV缓存随SWA收缩。最后一个对于Gemma-3-ish配置（1024窗口，128K上下文），KV缓存下降128x。

**质量成本。**仅swa变压器与远程检索斗争。解决方法：将SWA层与全注意力层交叉。Gemma 3使用5:1 SWA:global。Mistral 7B使用因果- swa堆栈，其中信息通过重叠的窗口“向前流动”-每层扩展有效的接收场

### 稀疏/块注意

选一个三个典型的形状：

- **本地+跨步（OpenAI稀疏变压器）。**注意最后一个捕捉本地和远程信号
- **Longformer / BigBird。**本地窗口+一小组全局令牌(例如：经验2x上下文匹配质量。
- **原生稀疏注意（DeepSeek, 2025）。**学习哪块跳过内核级别的零块。FlashAttention-compatible。

稀疏注意力是一个核工程的故事。数学很简单（掩盖分数矩阵）；胜利来自于从不将零条目加载到SRAM中。FlashAttention-3和2026 FlexAttention API使自定义稀疏模式在PyTorch中成为一级。

### 差分注意（DIFF变压器，2024）

常规关注有一个“注意力消耗”问题：softmax强制每行求和为1，因此不想关注任何特定内容的令牌会将权重倾倒在第一个令牌（或前几个）上。这窃取了本应用于真正内容的容量。

微分注意通过计算**两个**注意图并减去：

```
A1 = softmax(Q1 K1^T / √d)
A2 = softmax(Q2 K2^T / √d)
DiffAttn = (A1 - λ · A2) V
```

在哪里A1捕获真实内容权重；A2捕获接收器。减法取消下沉，将权重重新分配给相关令牌。

报告结果（Microsoft 2024）：困惑降低5-10%，相同训练长度的有效上下文延长1.5 - 2倍，更清晰的大海捞针检索。

### 变异的比较

| Variant | Compute | KV cache | Quality vs full | Production use |
|---------|---------|----------|-----------------|----------------|
| Full attention | O(N²) | O(N) per layer | baseline | every model's default layer |
| SWA (window 1024) | O(N·W) | O(W) per layer | -0.1 ppl, good with global layers | Gemma 2/3, Phi-3-Long |
| Local + strided sparse | O(N·√N) | mixed | similar to SWA | OpenAI sparse transformer, Longformer |
| BigBird (local + global + random) | O(N) approx | mixed | matches full at 2× context | early long-context BERT |
| Native Sparse (DeepSeek-V3.2) | O(N · active fraction) | O(N) | within 0.05 ppl | DeepSeek-V3.2, 2025 |
| Differential | O(2·N²) | O(2N) | -5 to -10% ppl | DIFF Transformer, early 2026 models |

## 构建它

参见我们实现了一个因果掩码比较器，它在一个玩具序列上并排显示完整、SWA、局部+跨步和微分注意力。

### 第一步：全因果面膜（基线）

”“python
def causal_mask (n):
返回[0.0 if j <= I else float("-inf") for j in range(n)] for I in range(n)]
```

Baseline from Lesson 07. Lower triangular; zero weight above the diagonal.

### Step 2: sliding window causal mask

```python
def swa_mask(n, window):
    M = [[float("-inf")] * n for _ in range(n)]
    for i in range(n):
        lo = max(0, i - window + 1)
        for j in range(lo, i + 1):
            M[i][j] = 0.0
    return M
```

一个参数——因为因为

### 步骤3：局部+跨步稀疏蒙版

”“python
Def strided_mask(n, window, stride)：
M = [[float("-inf")] * n for _ in range(n)]
对于I在(n)范围内：
Lo = max（0, I - window + 1）
对于range（lo, I + 1）中的j：
M[i][j] = 0.0
对于区间（0,I + 1, stride）中的j：
M[i][j] = 0.0
返回米
```

Dense local window plus every `stride`-th token back to the start of the sequence. Receptive field grows in log steps with additional layers.

### Step 4: differential attention

```python
def diff_attention(Q1, K1, Q2, K2, V, lam):
    A1 = softmax_causal(Q1 @ K1.T / sqrt_d)
    A2 = softmax_causal(Q2 @ K2.T / sqrt_d)
    return (A1 - lam * A2) @ V
```

两次注意过，用一个学过的混合系数减去。在代码中，我们比较了单个和差分的注意力池热图，并观察了注意力池的崩溃。

### 步骤5:KV缓存大小

打印每层缓存大小SWA和稀疏变体下降了10 - 100倍。微分双打。有意识地为你的记忆力买单。

## 使用它

2026生产格局：

”“python
从变压器导入AutoModelForCausalLM
# Gemma 3以5:1混合SWA（窗口=1024）和全局层。
model = AutoModelForCausalLM.from_pretrained（“谷歌/gemma-3-27b-it”）
# 打印(model.config。sliding_window model.config.layer_types)
```

FlexAttention in PyTorch 2.5+ accepts a mask function:

```python
from torch.nn.attention.flex_attention import flex_attention, create_block_mask

def swa_pattern(b, h, q_idx, kv_idx):
    return (q_idx - kv_idx < 1024) & (q_idx >= kv_idx)

mask = create_block_mask(swa_pattern, B=batch, H=heads, Q_LEN=n, KV_LEN=n)
out = flex_attention(q, k, v, block_mask=mask)
```

这将编译成自定义的Triton内核。在常见模式的FlashAttention-3速度的10%以内，并且mask函数是Python可调用的。

**何时挑选：**

- **纯粹的充分关注** -每层高达~16K上下文，或当检索质量是至关重要的。
- **SWA +全局混合** -长上下文（>32K），训练和推理内存约束。2026年默认32K以上。
- **稀疏块注意** -自定义内核，自定义模式。为专门的工作负载（检索、音频）保留。
- **注意力差异** -任何注意力汇污染的工作（长上下文RAG，大海捞针）。

## 船这

参见该技能为给定目标上下文长度、检索需求和训练/推理计算配置文件的新模型选择注意力拓扑。

## 练习

1. * *容易。**运行验证SWA验证
2. * *介质。**实施因果SWA骑着小莎士比亚走1000步。相对于完全的注意力，val损失会倒退多少？峰值内存下降了多少？
3. * *难。**在顶点模型中实现gemma -3风格的5:1层混合（5 SWA, 1 global）。在匹配的参数下，将损失、内存和生成质量与纯swa和纯全局基线进行比较。
4. * *难。**对学习过的孩子实施不同的注意力训练一个合成检索任务（一根针，2000个干扰物）。测量检索精度与匹配参数下的单注意基线。

## 关键术语

| Term | What people say | What it actually means |
|------|-----------------|-----------------------|
| Sliding window attention (SWA) | "Local attention" | Each query attends to its last `W` tokens; KV cache shrinks to `O(W)`. |
| Effective receptive field | "How far back the model sees" | In an `L`-layer SWA stack with window `W`, up to `L × W` tokens. |
| Longformer / BigBird | "Local + global + random" | Sparse patterns with a few always-attending global tokens; early long-context approach. |
| Native Sparse Attention | "DeepSeek's kernel trick" | Learn block-level sparsity; skip zero blocks at the kernel level while keeping quality. |
| Differential attention | "Two maps, one subtracts" | DIFF Transformer: subtract a learned `λ` times a second attention map from the first to cancel attention sinks. |
| Attention sink | "Weight bleeds to token 0" | Softmax normalization forces rows to sum to 1; uninformative queries dump weight on position 0. |
| FlexAttention | "Mask-as-Python" | PyTorch 2.5+ API that compiles arbitrary mask functions into FlashAttention-shape kernels. |
| Layer type mix | "5:1 SWA-to-global" | Interleave sparse and full attention layers in a stack to keep quality at lower memory. |

## 进一步的阅读

- Z0
- Z0
- Z0
- Z0
- Z0
- Z0
- Z0
- Z0