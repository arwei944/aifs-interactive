# 手套，快速文本和子词嵌入

> Word2Vec每个单词训练一个嵌入。GloVe对共生矩阵进行因式分解。FastText嵌入了这些碎片。BPE桥接Transformer。

**Type:** Build
**Languages:** Python
**Prerequisites:** Phase 5 · 03 (Word2Vec from Scratch)
**Time:** ~45 minutes

## 这个问题

Word2Vec留下了两个悬而未决的问题。

首先，有一条平行的研究线，直接分解共生矩阵（LSA， HAL），而不是进行在线跳克更新。是Word2Vec的迭代方法从根本上更好，还是这两种方法处理计数方式的差异？**GloVe**回答说：精心选择损失的矩阵分解可以匹配或击败Word2Vec，并且训练成本更低。

其次，这两种方法都没有见过的单词的故事。`Zoomer-approved`, `dogecoin`, any proper noun coined last week, every inflected form of a rare root. **FastText**通过嵌入字符n-grams解决了这个问题：一个单词是其各部分（包括语素）的总和，因此即使是超出词汇表的单词也会得到一个合理的向量。

第三，一旦Transformer到来，问题又转移了。单词级别的词汇最多可达100万个；真正的语言比这更开放。**字节对编码(BPE)**及其相关技术通过学习涵盖所有内容的频繁子词单位的词汇表来解决这个问题。每个现代LLM的每个现代标记器都是子词标记器。

这节课让三个人都走到一起，然后解释什么时候该拿哪个。

## 这个概念

**手套（全局矢量）。**建立词词共现矩阵这样训练向量权重损失如此频繁，对不占主导地位。完成了。

** FastText。一个单词是它的n个字符的和加上单词本身。`where` becomes `<wh, whe, her, ere, re>, <where>`. 向量这个词就是这些分量向量的和。训练为Word2Vec。好处：看不见的话

**BPE（字节对编码）。**从单个字节（或字符）的词汇表开始。计算语料库中的每个相邻对。将最频繁的对合并到一个新的令牌中。重复结果：词汇量每个句子都有符号。

## 构建它

### GloVe：分解共生矩阵

”“python
import numpy as np
从集合导入计数器


Def build_coconcurrency (docs, window=5)：
pair_counts = Counter（）
词汇= {}
对于文档中的文档：
对于doc中的token：
如果符号不在词汇中：
Vocab [token] = len（Vocab）
对于文档中的文档：
索引= [vocab[t] for t in doc]
对于i，在enumerate（索引）中居中：
For j in range(max(0, I - window), min(len(index), I + window + 1))：
如果我！= j:
距离= abs（i - j）
Pair_counts [(center, indexed[j])] += 1.0 / distance
返回词汇表，pair_counts


Def glove_train(vocab, pair_counts, dim=16, epochs=100, lr=0.05, x_max=100, alpha=0.75, seed=0)：
N = len（词汇）
RNG = np.random.default_rng（种子）
W = rng。Normal （0,0.1, size=(n, dim)）
W_tilde = rng。Normal （0,0.1, size=(n, dim)）
B = np. 0 (n)
B_tilde = np. 0 (n)

对于range（epochs）中的epoch：
For (i, j), x_ij in pair_counts.items()：
权重= (x_ij / x_max) ** alpha如果x_ij < x_max else 1.0
diff = W[我]@ W_tilde [j] + b[我]+ b_tilde [j], np.log (x_ij)
Coef =重量*差异

            grad_W_i = coef * W_tilde[j]
            grad_W_tilde_j = coef * W[i]
            W[i] -= lr * grad_W_i
            W_tilde[j] -= lr * grad_W_tilde_j
            b[i] -= lr * coef
            b_tilde[j] -= lr * coef

返回W + w_波浪
```

Two moving pieces worth naming. The weighting function `f(x) = (x/x_max)^alpha` downweights very frequent pairs (like `(the, and)`) so they do not dominate the loss. The final embedding is the sum of `W` (center) and `W_tilde` (context) tables. Summing both is a published trick that tends to outperform using just one.

### FastText: subword-aware embeddings

```python
def char_ngrams(word, n_min=3, n_max=6):
    wrapped = f"<{word}>"
    grams = {wrapped}
    for n in range(n_min, n_max + 1):
        for i in range(len(wrapped) - n + 1):
            grams.add(wrapped[i:i + n])
    return grams
```

”“python
>> > char_ngrams(“”)
{' <, >”、“< wh”、“为什么”、“她”、“在”、“re >”、“<”、“时”、“这里”,“从哪里之前>”、“<”、“在哪儿”、“这里> '}
```

Each word is represented by its set of n-grams (typically 3 to 6 characters). The word embedding is the sum of its n-gram embeddings. For skip-gram training, plug this in where Word2Vec used a single vector.

```python
def fasttext_vector(word, ngram_table):
    grams = char_ngrams(word)
    vecs = [ngram_table[g] for g in grams if g in ngram_table]
    if not vecs:
        return None
    return np.sum(vecs, axis=0)
```

对于一个看不见的词，你仍然得到一个向量，只要它的一些n-gram是已知的。`whereupon` shares `<wh`, `her`, `ere`, and `<where` with `where`, so the two land near each other.

### BPE：学过的次词词汇

”“python
Def learn_bpe(corpus, k_merges)：
vocab = Counter（）
对于单词，在corpus.items（）中的频率：
token = tuple(word) + （"</w>",）
Vocab [token] = frequency

合并= []
对于_in (k_merges)：
pair_freq = Counter（）
对于记号，在vocabulary .items（）中的频率：
For a, b in zip(tokens, tokens[1:])：
Pair_freq [(a, b)] += freq
如果不是pair_freq：
打破
Best = pair_frequency .most_common(1)[0][0]
merges.append(最好的)

new_vocab = Counter（）
对于记号，在vocabulary .items（）中的频率：
New_tokens = []
I = 0
当I < len(tokens)：
如果I + 1 < len(tokens) and (tokens[I], token [I + 1]) == best：
new_tokens。Append （tokens[i] + token [i + 1]）
I += 2
其他:
new_tokens.append(令牌[我])
I += 1
New_vocab [tuple(new_tokens)] =频率
Vocab = new_vocab
返回合并


Def apply_bpe(word, merge)：
token = list(word) + ["</w>"]
对于a， b在归并中：
New_tokens = []
I = 0
当I < len(tokens)：
如果I + 1 < len(tokens) and tokens[I] == a and tokens[I + 1] == b：
new_tokens。追加（a + b）
I += 2
其他:
new_tokens.append(令牌[我])
I += 1
token = new_token
返回标记
```

```python
>>> corpus = Counter({"low": 5, "lower": 2, "newest": 6, "widest": 3})
>>> merges = learn_bpe(corpus, k_merges=10)
>>> apply_bpe("lowest", merges)
['low', 'est</w>']
```

第一次迭代合并最常见的相邻对。经过足够的迭代，频繁的子字符串(

真正的GPT / BERT / T5标记器学习30k-100k个合并。结果：任何文本都被标记为已知id的有限长度序列，没有OOV。

## 使用它

在实践中，你很少自己训练这些技能。你装载预先训练好的检查点。

”“python
进口fasttext.util
fasttext.util。download_model(“en”,if_exists =“忽略”)
Ft = fasttext.load_model（"cc.en.300.bin"）
print (ft.get_word_vector(“于是”).shape)
print (ft.get_word_vector(“zoomerapproved”).shape)
```

For BPE-style subword tokenization in the transformer era:

```python
from transformers import AutoTokenizer

tok = AutoTokenizer.from_pretrained("gpt2")
print(tok.tokenize("unbelievably tokenized"))
```

```
['un', 'bel', 'iev', 'ably', 'Ġtoken', 'ized']
```

The每个现代标记器都是BPE的变体，WordPiece （BERT）或sentencepece （T5, LLaMA）。

### 什么时候选哪个

| Situation | Pick |
|-----------|------|
| Pretrained general-purpose word vectors, no OOV tolerance needed | GloVe 300d |
| Pretrained general-purpose word vectors, must handle misspellings / neologisms / morphologically rich languages | FastText |
| Anything going into a transformer (training or inference) | Whatever tokenizer the model shipped with. Never swap. |
| Training your own language model from scratch | Train a BPE or SentencePiece tokenizer on your corpus first |
| Production text classification with a linear model | Still TF-IDF. Lesson 02. |

## 船这

另存为

”“减价
---
名称:tokenizer-picker
描述：为新的语言模型或文本管道选择一种标记化方法。
版本:1.0.0
阶段:5
教训:04
标签：[nlp，标记化，嵌入]
---

给定任务和数据集描述，输出：

1. 标记化策略（词级、BPE、WordPiece、sentencepece、字节级）。一句话的原因。
2. 词汇量目标（例如，纯英语LM为32k，多语言LM为64k-100k）。
3. 使用精确的训练命令调用库。命名库。引用论点。
4. 一个可再现性缺陷。Tokenizer-model不匹配是最常见的无声生产bug；说出哪一对必须一起使用。

当用户对预训练的LLM进行微调时，拒绝推荐训练自def 标记器。拒绝为任何以生产推理为目标的模型推荐词级标记化。将非英语/多脚本语料库标记为需要带字节回退的sentencepece。
```

## Exercises

1. **Easy.** Run `char_ngrams("playing")` and `char_ngrams("played")`. Compute the Jaccard overlap of the two n-gram sets. You should see substantial shared pieces (`pla`, `lay`, `play`), which is why FastText transfers well across morphological variants.
2. **Medium.** Extend `learn_bpe` to track vocabulary growth. Plot tokens-per-corpus-character as a function of number of merges. You should see rapid compression at first, asymptoting near ~2-3 chars per token.
3. **Hard.** Train a 1k-merge BPE on Shakespeare's complete works. Compare tokenization of common words vs. rare proper nouns. Measure average tokens per word before and after. Write up what surprised you.

## Key Terms

| Term | What people say | What it actually means |
|------|-----------------|-----------------------|
| Co-occurrence matrix | Word-word frequency table | `X[i][j]` = how often word `j` appears in a window around word `i`. |
| Subword | Piece of a word | A character n-gram (FastText) or learned token (BPE/WordPiece/SentencePiece). |
| BPE | Byte-pair encoding | Iterative merging of most-frequent adjacent pairs until vocabulary hits target size. |
| OOV | Out of vocabulary | Word the model has never seen. Word2Vec/GloVe fail. FastText and BPE handle it. |
| Byte-level BPE | BPE on raw bytes | GPT-2's scheme. Vocabulary starts with 256 bytes, so nothing is ever OOV. |

## Further Reading

- [Pennington, Socher, Manning (2014). GloVe: Global Vectors for Word Representation](https://nlp.stanford.edu/pubs/glove.pdf) — the GloVe paper, seven pages, still the best derivation of the loss.
- [Bojanowski et al. (2017). Enriching Word Vectors with Subword Information](https://arxiv.org/abs/1607.04606) — FastText.
- [Sennrich, Haddow, Birch (2016). Neural Machine Translation of Rare Words with Subword Units](https://arxiv.org/abs/1508.07909) — the paper that introduced BPE to modern NLP.
- [Hugging Face tokenizer summary](https://huggingface.co/docs/transformers/tokenizer_summary) — how BPE, WordPiece, and SentencePiece actually differ in practice.