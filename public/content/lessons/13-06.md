# MCP基础-原语，生命周期，JSON-RPC基础

> MCP之前的每一次整合都是一次性的。模型上下文协议于2024年11月由Anthropic首次发布，现在由Linux基金会的代理人工智能基金会管理，它标准化了发现和调用，因此任何客户端都可以与任何服务器通话。2025-11-25规范命名了六个原语（三个服务器、三个客户端）、一个三阶段生命周期和一个JSON-RPC 2.0连接格式。学习这些内容，这一阶段的MCP章节的其余部分就变成了阅读。

**Type:** Learn
**Languages:** Python (stdlib, JSON-RPC parser)
**Prerequisites:** Phase 13 · 01 through 05 (the tool interface and function calling)
**Time:** ~45 minutes

## 学习目标

- 命名所有六个MCP原语（服务器上的工具、资源、提示；客户机上的根、采样、启发），并分别给出一个用例。
- 遍历三个阶段的生命周期（初始化、操作、关闭），并说明谁在每个阶段发送哪条消息。
- 解析并发出JSON-RPC 2.0请求、响应和通知信封。
- 解释什么是能力谈判

## 这个问题

在MCP之前，每个使用工具的代理都有自己的协议。光标有一个mcp形状但不兼容的工具系统。克劳德·台普顿带了另一个。VS Code的Copilot扩展有第三个。一个构建“Postgres查询”工具的团队将同一工具编写了三次，每次都针对不同的主机API。重用它需要复制代码。

结果是寒武纪一次性整合的大爆发和生态系统速度的上限。

MCP通过标准化线路格式解决了这个问题。单个MCP服务器适用于每个MCP客户端：Claude Desktop， ChatGPT, Cursor, VS Code, Gemini, Goose, Zed, Windsurf，到2026年4月将有300多个客户端。每月1.1亿次SDK下载。1万多个公共服务器。Linux基金会于2025年12月接管了新成立的代理人工智能基金会。

此阶段使用的规范修订为**2025-11-25**。它增加了异步任务（SEP-1686）、url模式激发（SEP-1036）、工具抽样（SEP-1577）、增量范围同意（SEP-835）和OAuth 2.1资源指示符语义。第13.09至第16阶段涵盖这些扩展。这节课到此结束。

## 这个概念

### 三个服务器原语

1. * *的工具。**可调用的操作。与第13·01阶段相同的四步循环。
2. * *资源。**暴露的数据。可通过URI寻址的只读内容：
3. * *提示。**可重用的模板。主机UI中的斜杠命令；服务器提供模板，客户端填充参数。

### 三个客户端原语

4. * *的根源。**允许服务器访问的uri集合。客户声明他们；服务器尊重他们。
5. * *抽样。**服务器请求客户端模型执行补全。启用没有服务器端API密钥的服务器托管代理循环。
6. * *引出。**服务器要求客户端用户进行结构化输入。表单或url （SEP-1036）。

MCP中的每个功能都属于这六个中的一个。第13·10至14阶段分别深入介绍。

### 连线格式：JSON-RPC 2.0

每条消息都是一个JSON对象，包含以下字段：

- 请求：
- 回应：
- 通知：

基本规范有大约15个方法，按原语分组。重要的是：

- Z0
- Z0
- Z0
- Z0
- Z0
- Z0

### 三相生命周期

**阶段1：初始化

客户端发送服务器用它自己的响应客户端发送从这里开始，任何一方都可以根据协商的功能发送请求。

**第二阶段：运营

双向的。客户端呼叫服务器可以发送服务器可以发送客户端可以发送

**阶段3：关机

任何一方关闭运输。MCP无结构关断方法；传输（stdio或Streamable HTTP，阶段13.09）携带连接结束信号。

### 谈判能力

Z0来自服务器的示例：

' ' ' json
｛
“tools”： {"listChanged": true}，
“resources”： {"subscribe": true, "listChanged": true}，
“prompts”： {"listChanged": true}
｝
```

The server declares it can emit `tools/list_changed` notifications and supports `resources/subscribe`. The client agrees by declaring its own:

```json
{
  "roots": {"listChanged": true},
  "sampling": {},
  "elicitation": {}
}
```

如果客户没有申报对称：如果服务器没有声明

这就是防止生态系统漂移的原因。不支持采样的客户端仍然是有效的MCP客户端；不调用的服务器他们只是没有一起使用这个功能。

### 结构化内容和错误形状

Z0第13·14阶段增加了MCP应用程序(

错误使用JSON-RPC错误码。具体定义的添加项：

### 客户端功能vs工具调用细节

常见的混淆：客户端是否会调用特定的工具是由其模型驱动的运行时选择，而不是能力标志。能力标志是规范级别的契约。模型的选择是正交的。

### 为什么是JSON-RPC而不是REST？

JSON-RPC 2.0（2010）是一个轻量级的双向协议。REST是客户端发起的。MCP需要服务器发起的消息（采样、通知），因此具有对称请求/响应形状的JSON-RPC是一个自然的选择。JSON-RPC还可以在studio和WebSocket/Streamable HTTP上干净地组合，而无需重新发明HTTP的请求形状。

## 使用它

Z0没有真正的交通工具；只是信息的形状。与进一步阅读中链接的规范进行比较，以验证每个信封。

看点：

- Z0反应是
- Z0每个条目都有
- Z0
- 回应

## 船这

这一课产生了给定MCP客户机-服务器交互的pcap样式的抄本，该技能用哪个原语、哪个生命周期阶段以及它所依赖的哪个功能注释每个消息。

## 练习

1. 运行确定发生功能协商的行，并描述如果服务器没有声明会发生什么变化

2. 扩展解析器以处理信息的形状：在长时间运行时发出

3. 从头到尾阅读MCP 2025-11-25规范——整个文档大约有80页。确定一个大多数服务器不需要的功能标志。提示：它与资源订阅有关。

4. 在纸上画出假设的“cron job”特性所属的基本类型。(提示：服务器希望客户机在预定时间调用它。这六种原语都不适合今天。)MCP的2026年路线图对此有一个SEP草案。

5. 解析一个会话日志从一个开放的MCP服务器在GitHub上。对请求、响应和通知消息进行计数。计算生命周期和运营占流量的比例。

## 关键术语

| Term | What people say | What it actually means |
|------|----------------|------------------------|
| MCP | "Model Context Protocol" | Open protocol for model-to-tool discovery and invocation |
| Server primitive | "What a server exposes" | tools (actions), resources (data), prompts (templates) |
| Client primitive | "What a client lets servers use" | roots (scope), sampling (LLM callbacks), elicitation (user input) |
| JSON-RPC 2.0 | "The wire format" | Symmetric request/response/notification envelopes |
| `initialize` handshake | "Capability negotiation" | First message pair; servers and clients declare features they support |
| `tools/list` | "Discovery" | Client asks server for its current tool set |
| `tools/call` | "Invocation" | Client asks server to execute a tool with arguments |
| `notifications/*_changed` | "Mutation events" | Server tells client that its primitive list has changed |
| Content block | "Typed result" | `{type: "text" | "image" | "resource" | "ui_resource"}` in tool result |
| SEP | "Spec Evolution Proposal" | Named draft proposal (e.g. SEP-1686 for async Tasks) |

## 进一步的阅读

- Z0
- Z0
- Z0
- Z0
- Z0