# 代理框架的权衡——LangGraph vs CrewAI vs AutoGen vs Agno

> 每个框架都出售相同的演示（研究代理构建报告）并隐藏相同的错误（与编排层的状态模式冲突）。选择抽象与你的问题相匹配的框架；其他的都是胶水，你要写两遍。

**Type:** Learn
**Languages:** Python
**Prerequisites:** Phase 11 · 09 (Function Calling), Phase 11 · 16 (LangGraph)
**Time:** ~45 minutes

## 这个问题

您有一个任务需要多个LLM调用。也许这是一个研究工作流程（计划，搜索，总结，引用）。也许它是一个代码审查管道（解析、修改、修补、验证）。也许它是一个多轮助理，预订机票，写电子邮件，并归档费用报告。你选择一个框架。

三天后，您发现框架的抽象泄漏。CrewAI给你分配角色，但当“研究者”需要向“作者”提交一份结构化计划时，CrewAI会反对你。AutoGen让您在代理之间进行聊天，但没有一级状态，因此您的检查点是对话日志的一个pickle。LangGraph为您提供了一个状态图，但在您知道代理将做什么之前，强制您为每个转换命名。Agno为您提供了一个单代理原语，当您尝试分散到三个并发工作线程时，它会发出警报。

解决方法不是“选择最好的框架”。它是将框架的核心抽象与问题的形式相匹配。这一课描绘了这幅地图。

## 这个概念

Z0

四大框架主导着2026年的前景。它们的核心抽象是不一样的。

| Framework | Core abstraction | Best fit | Worst fit |
|-----------|------------------|----------|-----------|
| **LangGraph** | `StateGraph` — typed state, nodes, conditional edges, checkpointer. | Workflows with explicit state and human-in-the-loop interrupts; production agents needing time-travel debugging. | Loose, role-driven brainstorming where the topology is unknown. |
| **CrewAI** | `Crew` — roles (goal, backstory), tasks, process (sequential or hierarchical). | Role-playing or persona-driven workflows with a short linear/hierarchical plan. | Anything stateful beyond the crew's turn history; complex branching. |
| **AutoGen** | `ConversableAgent` pair — two or more agents that speak in turns until an exit condition. | Multi-agent *dialogue* (teacher-student, proposer-critic, actor-reviewer) where the thinking emerges from the chat. | Deterministic workflows with a known DAG; anything needing durable state across restarts. |
| **Agno** | `Agent` — a single LLM + tools + memory, composable into teams. | Fast-to-build single agents and lightweight teams; strong multi-modality and built-in storage drivers. | Deep, explicitly-branched graphs with custom reducers. |

### “抽象”到底是什么意思

框架的核心抽象是你在推销架构时在白板上画出来的东西。

- **LangGraph**→你画个图。节点是步骤，边是转换，每个点的状态对象都是类型的。心智模型是一个状态机。
- 你画一张组织结构图。每个角色都有一个职位描述，并且有一个经理负责分配任务。心智模型是一个专家小组。
- **AutoGen**→您绘制Slack DM。两个代理相互发送消息；如果你需要一个主持人，第三个加入。心理模型是聊天。
- **Agno**→你画一个单独的盒子，上面挂着工具。把盒子放在一个团队的旁边。心智模型是“包含电池的代理”。

### 国家问题

状态是大多数框架选择在生产中失败的地方。

- * * LangGraph。**类型状态(简历、打断和时间旅行都是免费的。*（见第11.16期）*
- * * CrewAI。状态流作为字符串在任务之间通过没有现成的耐用的乘员仓库；如果机组人员必须在重启中幸存下来，你必须自己逃跑。
- * * AutoGen。**状态为聊天记录和自定义状态对话记录保存下来；除非您编写适配器，否则任意工作流状态不会存在。
- * * Agno。**内置存储驱动（SQLite, Postgres, Mongo, Redis, DynamoDB）附加到一个不是一个完整的图形检查指针；会话存储。

### 分支问题

每个非平凡代理都有分支。谁来决定分支机构的重要性。

- **LangGraph** -你决定，通过条件边。Routing是一个带有命名分支的Python函数。分支在编译后的图中是一级的；检查指针记录了哪个分支被占用。
- **CrewAI**——管理者在层级模式下决定；在顺序模式中，您在构建时决定。路由隐含在任务列表中；在经理的提示之外，没有第一流的“如果”。
- **AutoGen** -代理通过聊天决定。分支是由下一个说话的人产生的。`GroupChatManager` selects the next speaker; 你可以手写一个
- **Agno** -代理决定下一步调用哪个工具。团队有协调器/路由器/协作器模式；超出这个范围的分支是开发人员的责任。

### 可观测性问题

- **LangGraph** - opentelementetry通过LangSmith或任何酒店出口商。每个节点转换都是一个跟踪跨度；检查点兼作可重放的轨迹。LangSmith是第一方的选择；Langfuse/Phoenix也有适配器。
- **人员** -自2025年底以来，一流的开放元素；集成Langfuse， Phoenix, Opik, AgentOps。
- **AutoGen** - OpenTelemetry集成通过AgentOps和Opik有连接器。跟踪粒度是每个代理消息，而不是每个节点。
- **Agno** -内置紧密集成与Langfuse的会话跟踪。

### 成本和延迟

所有四个框架都增加了每次调用的开销（框架逻辑、验证、序列化）。开销增加的大致顺序：Agno≈LangGraph < CrewAI≈AutoGen。差异主要取决于框架做了多少额外的LLM路由。CrewAI的分级管理器花费代币来决定谁是下一个；AutoGen的LangGraph只在你写的地方花代币Agno的单代理路径很单薄。

当每次运行成本很重要时，更倾向于显式路由(LangGraph边，AutoGen

### 互操作性

- **LangGraph**↔**LangChain**工具，检索器，llm一级MCP适配器（作为MCP服务器导入的工具）。
- **CrewAI**↔工具从LangChain工具、LlamaIndex工具和MCP工具都可以适应。机组间的委托
- 自动生成**→MCP适配器可用。为代理到代理模式与AG2生态系统紧密耦合。
- **Agno**→MCP适配器;工具可以在代理和团队之间共享。

## 的技能

> 您可以用一句话解释为什么给定的框架适用于给定的代理问题。

预构建清单:

1. **绘制形状。**这是一个图形（类型状态，命名转换）吗？角色扮演（专家交接工作）？聊天（代理交谈直到完成）？一个带着工具的特工？
2. **决定分支机构。**开发者决定分支→LangGraph。经理-代理-决定→船员等级。聊天紧急→AutoGen。工具调用决定→Agno。
3. **检查国家预算。**您需要检查点的简历吗？时光旅行吗?中途人为干扰？如果是，LangGraph是默认的；Agno会话涵盖会话范围的状态。
4. **检查成本预算。** llm选择的路由每回合需要额外的令牌。如果代理每天运行数千次，则首选显式路由。
5. **预算框架开销。每个框架都是另一个依赖项。如果任务是两个LLM调用和一个工具，写30行普通Python；没有框架比没有框架更便宜。

在您可以绘制图形、组织结构图、聊天或代理框之前，拒绝使用框架。拒绝选择一个强迫您为实际需要的东西而与其状态模型作斗争的工具。

## 决策矩阵

| Problem shape | Preferred framework | Why |
|---------------|---------------------|-----|
| Workflow DAG with typed state, human approvals, long-running | LangGraph | First-class state, checkpointer, interrupts, time-travel. |
| Research / writing pipeline with distinct roles | CrewAI (sequential) or LangGraph subgraphs | Role-per-task is cheap to express in CrewAI; scale up with LangGraph when branching gets complex. |
| Proposer-critic or teacher-student dialogue | AutoGen | Two-agent chat is its native shape. |
| Single agent with tools, sessions, memory | Agno | Thinnest setup, built-in storage and memory. |
| Thousands of parallel fanouts with reducers | LangGraph + `Send` | The only one with a first-class parallel dispatch primitive. |
| Quick prototype, no framework commitment | Plain Python + provider SDK | No framework is the fastest framework. |

## 练习

1. * *容易。**以同样的任务——“研究Anthropic的总部，写一篇200字的摘要，引用来源”——并在LangGraph（四个节点：计划、搜索、写作、引用）和CrewAI（三个角色：研究员、作家、编辑）中实现它。报告每次运行的令牌成本和代码行数。
2. * *介质。**在AutoGen中建立相同的任务（研究者↔撰写者聊天，编辑者通过连接）根据(a)每次运行成本、(b)崩溃后恢复的能力、(c)在写入步骤之前注入人工批准的能力对四种实现进行排名。
3. * *难。**构建决策树脚本在你自己设计的六个案例中验证它。

## 关键术语

| Term | What people say | What it actually means |
|------|-----------------|-----------------------|
| Orchestration | "How the agents coordinate" | The layer that decides which node/role/agent runs next. |
| Durable state | "Resume after a restart" | State that survives process death, attached to a checkpoint or session store. |
| LLM-selected routing | "Let the model decide" | A planner LLM picks the next step each turn; flexible but pays tokens on every decision. |
| Explicit routing | "Developer decides" | A Python function or static edge picks the next step; cheap and auditable. |
| Crew | "A CrewAI team" | Roles + tasks + process (sequential or hierarchical) bound into a single runnable. |
| GroupChat | "AutoGen's multi-agent chat" | A managed conversation between N agents with a speaker selector. |
| Team (Agno) | "Multi-agent Agno" | Route / coordinate / collaborate mode over a set of agents. |
| StateGraph | "LangGraph's graph" | Typed-state, node, conditional-edge, checkpointer primitive. |

## 进一步的阅读

- Z0
- Z0
- Z0
- Z0
- Z0
- Z0
- Z0
- Z0
- 第11.16阶段（LangGraph）——本课程的基准框架。
- 阶段11.19（反射）——一个模式可以清晰地映射到LangGraph，但很难映射到CrewAI。
- 阶段11.22（产品可观察性）-如何检测您选择的任何框架。