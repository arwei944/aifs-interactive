# 词性标注和句法分析

> 有一段时间，语法并不流行。然后每个LLM管道都需要验证结构化提取，然后再返回。

**Type:** Build
**Languages:** Python
**Prerequisites:** Phase 5 · 01 (Text Processing), Phase 2 · 14 (Naive Bayes)
**Time:** ~45 minutes

## 这个问题

第01课告诉我们，词形化需要一个词性标签。不知道不知道

这个承诺隐藏了整个子领域。词性标注指定语法范畴。句法分析恢复句子的树状结构：哪个词修饰哪个词，哪个动词支配哪个论点。经典NLP花了20年的时间来完善这两者。然后，深度学习将它们分解为一个预训练的转换器之上的令牌分类任务，研究团体继续前进。

而不是应用社区。每个结构化提取管道在底层仍然使用POS和依赖树。llm生成的JSON根据语法约束进行验证。问答系统使用依赖解析分解查询。机器翻译质量评估员检查解析树的对齐。

值得了解。本课将介绍标记集、基线以及停止从头实现并调用space的点。

## 这个概念

**POS标记**标记每个标记与语法类别。**Penn Treebank (PTB)**标签集是英语的默认设置。36个带有区别的标签，一般读者会觉得很繁琐：**Universal Dependencies (UD)**标签集比较粗糙（17个标签）并且与语言无关；它成为跨语言工作的默认设置。

```
The/DET cats/NOUN were/AUX running/VERB at/ADP 3pm/NOUN ./PUNCT
```

*语法解析**生成树。两大风格：

- **选区解析。名词短语、动词短语、介词短语相互嵌套。输出是非终端类别（NP， VP， PP）的树，以单词作为叶子。
- **依赖解析。**每个单词都有一个它所依赖的头词，并标有语法关系。输出是一个树，其中每条边都是（头部，依赖，关系）三重。

依赖解析在2010年代大获全胜，因为它可以清晰地泛化语言，尤其是自由词序语言。

```
running is ROOT
cats is nsubj of running
were is aux of running
at is prep of running
3pm is pobj of at
```

## 构建它

### 步骤1：最频繁标签基线

最愚蠢的POS标记器。对于每个单词，预测它在训练中最常使用的标签。

”“python
从集合中导入计数器


def train_mft (train_examples):
word_tag_counts = defaultdict（计数器）
all_tags = Counter（）
对于令牌，train_examples中的标签：
对于token， zip中的tag (tokens, tags)：
word_tag_counts(令牌。Lower ()][tag] += 1
All_tags [tag] += 1
Word_best = {w: c.most_common(1)[0][0] for w, c in word_tag_counts.items()}
Default_tag = all_tags.most_common(1)[0][0]
返回word_best， default_tag


Def predict_mft(tokens, word_best, default_tag)：
返回(word_best.get (t。Lower (), default_tag) for t in tokens]
```

On the Brown corpus, this baseline hits ~85% accuracy. Not good, but the floor below which no serious model should fall.

### Step 2: bigram HMM tagger

Model the joint probability of the sequence:

```
P(tags, words) = prod P(tag_i | tag_{i-1}) * P（word_i | tag_i）
```

Two tables: transition probabilities (tag given previous tag), emission probabilities (word given tag). Estimate both from counts with Laplace smoothing. Decode with Viterbi (dynamic programming over the tag lattice).

```python
import math


def train_hmm(train_examples, alpha=0.01):
    transitions = defaultdict(Counter)
    emissions = defaultdict(Counter)
    tags = set()
    vocab = set()

    for tokens, ts in train_examples:
        prev = "<BOS>"
        for token, tag in zip(tokens, ts):
            transitions[prev][tag] += 1
            emissions[tag][token.lower()] += 1
            tags.add(tag)
            vocab.add(token.lower())
            prev = tag
        transitions[prev]["<EOS>"] += 1

    return transitions, emissions, tags, vocab


def log_prob(table, given, key, smooth_denom, alpha):
    return math.log((table[given].get(key, 0) + alpha) / smooth_denom)


def viterbi(tokens, transitions, emissions, tags, vocab, alpha=0.01):
    tags_list = list(tags)
    n = len(tokens)
    V = [[0.0] * len(tags_list) for _ in range(n)]
    back = [[0] * len(tags_list) for _ in range(n)]

    for j, tag in enumerate(tags_list):
        em_denom = sum(emissions[tag].values()) + alpha * (len(vocab) + 1)
        tr_denom = sum(transitions["<BOS>"].values()) + alpha * (len(tags_list) + 1)
        tr = log_prob(transitions, "<BOS>", tag, tr_denom, alpha)
        em = log_prob(emissions, tag, tokens[0].lower(), em_denom, alpha)
        V[0][j] = tr + em
        back[0][j] = 0

    for i in range(1, n):
        for j, tag in enumerate(tags_list):
            em_denom = sum(emissions[tag].values()) + alpha * (len(vocab) + 1)
            em = log_prob(emissions, tag, tokens[i].lower(), em_denom, alpha)
            best_prev = 0
            best_score = -1e30
            for k, prev_tag in enumerate(tags_list):
                tr_denom = sum(transitions[prev_tag].values()) + alpha * (len(tags_list) + 1)
                tr = log_prob(transitions, prev_tag, tag, tr_denom, alpha)
                score = V[i - 1][k] + tr + em
                if score > best_score:
                    best_score = score
                    best_prev = k
            V[i][j] = best_score
            back[i][j] = best_prev

    last_best = max(range(len(tags_list)), key=lambda j: V[n - 1][j])
    path = [last_best]
    for i in range(n - 1, 0, -1):
        path.append(back[i][path[-1]])
    return [tags_list[j] for j in reversed(path)]
```

Bigram HMM在Brown上命中~93%的准确率。从85%到93%的跳跃主要是转移概率——模型学习

### 第三步：为什么现代的标签器会打败这个

跃迁和发射概率是局部的。他们无法捕捉到带有任意特征（后缀、词形、单词前后、单词本身）的CRF达到了97%。一个BiLSTM-CRF或Transformer达到~98%+。

这个任务的上限由注释者的分歧决定。人类注释者在Penn Treebank上的一致性约为97%。超过98%的模型可能是过拟合的测试集。

### 步骤4：依赖解析草图

从头开始的完全依赖解析超出了范围；典型的教科书式的处理是在Jurafsky和Martin的书中。要知道的两个经典家庭：

- 基于转换的解析器（arc-eager, arc-standard）的行为类似于shift-reduce解析器：它们读取令牌，将它们移到堆栈上，并应用reduce操作来创建arc。贪婪解码速度快。经典的实现是MaltParser。现代神经网络版本：Chen和Manning的基于转换的解析器。
- 基于图的解析器（Eisner的算法，Dozat-Manning biaffine）对每个可能的头部相关边进行评分，并选择最大生成树。更慢，但更准确。

对于大多数应用工作，请调用spaCy：

”“python
进口宽大的

NLP = space .load（"en_core_web_sm"）
doc = nlp（“猫在下午3点跑步。”）
对于doc中的token：
打印(f”{令牌。文本:10年代}标签={令牌。pos tag_: 5 s} ={令牌。Pos_:6s}深度={token.dep_:10s}头部={token.head.text}")
```

```
tag=DT pos=DET dep= DET head=cats
cats tag=NNS pos=名词deep =nsubj head=running
tag=VBD pos=AUX depth =AUX head=running
running tag=VBG pos=VERB deep =ROOT head=running
at tag=IN pos=ADP dep=prep head=running
3pm tag=NN pos=名词deep =pobj head=at
。标签=。点=点深=点头=跑
```

Read the `dep` column bottom to top and the sentence's grammatical structure falls out.

## Use It

Every production NLP library ships POS and dependency parsers as part of a standard pipeline.

- **spaCy** (`en_core_web_sm` / `md` / `lg` / `trf`). Fast, accurate, integrated with tokenization + NER + lemmatization. `token.tag_` (Penn), `token.pos_` (UD), `token.dep_` (dependency relation).
- **Stanford NLP (stanza)**. Stanford's successor to CoreNLP. State-of-the-art on 60+ languages.
- **trankit**. Transformer-based, good UD accuracy.
- **NLTK**. `pos_tag`. Usable, slow, older. Fine for teaching.

### Where this still matters in 2026

- **Lemmatization.** Lesson 01 needs POS to lemmatize correctly. Always.
- **Structured extraction from LLM outputs.** Validate that a generated sentence respects grammatical constraints (e.g., subject-verb agreement, required modifiers).
- **Aspect-based sentiment.** Dependency parses tell you which adjective modifies which noun.
- **Query understanding.** "movies directed by Wes Anderson starring Bill Murray" decomposes into structured constraints via the parse.
- **Cross-lingual transfer.** UD tags and dependency relations are language-agnostic, enabling zero-shot structured analysis of new languages.
- **Low-compute pipelines.** If you cannot ship a transformer, POS + dependency parse + gazetteer gets you surprisingly far.

## Ship It

Save as `outputs/skill-grammar-pipeline.md`:

```markdown
---
name: grammar-pipeline
description: Design a classical POS + dependency pipeline for a downstream NLP task.
version: 1.0.0
phase: 5
lesson: 07
tags: [nlp, pos, parsing]
---

Given a downstream task (information extraction, rewrite validation, query decomposition, lemmatization), you output:

1. Tagset to use. Penn Treebank for English-only legacy pipelines, Universal Dependencies for multilingual or cross-lingual.
2. Library. spaCy for most production, stanza for academic-grade multilingual, trankit for highest UD accuracy. Name the specific model ID.
3. Integration pattern. Show the 3-5 lines that call the library and consume the needed attributes (`.pos_`, `.dep_`, `.head`).
4. Failure mode to test. Noun-verb ambiguity (`saw`, `book`, `can`) and PP-attachment ambiguity are the classical traps. Sample 20 outputs and eyeball.

Refuse to recommend rolling your own parser. Building parsers from scratch is a research project, not an application task. Flag any pipeline that consumes POS tags without handling lowercase/uppercase variants as fragile.
```

## 练习

1. **容易。**在一个小的标记语料库（例如，NLTK的Brown子集）上使用最频繁标签基线，测量搁置句子的准确性。验证~85%的结果。
2. **介质。**训练上面的二元HMM，并报告每个标签的精度/召回率。HMM最容易混淆哪些标签？
3. **难。**使用spaCy的依赖解析从1000个句子的样本中提取主语-动词-对象三元组。评估50个手动标记的三元组。提取失败的文档（通常是被动、协调和省略的主题）。

## 关键术语

| Term | What people say | What it actually means |
|------|-----------------|-----------------------|
| POS tag | Word's type | Grammatical category. PTB has 36; UD has 17. |
| Penn Treebank | Standard tagset | English-specific. Fine-grained verb tenses and noun number. |
| Universal Dependencies | Multilingual tagset | Coarser than PTB; language-neutral; defaults for cross-lingual work. |
| Dependency parse | Sentence tree | Each word has one head, each edge has a grammatical relation. |
| Viterbi | Dynamic programming | Finds the highest-probability tag sequence given emissions and transitions. |

## 进一步的阅读

- 
- 
- Z1
- 