# Claude Agent SDK：子代理和会话存储

> Claude Agent SDK是Claude Code harness的库形式。内置工具、上下文隔离子代理、钩子、W3C跟踪传播、会话存储奇偶校验。Claude托管代理是长时间运行的异步工作的托管替代方案。

**Type:** Learn + Build
**Languages:** Python (stdlib)
**Prerequisites:** Phase 14 · 01 (Agent Loop), Phase 14 · 10 (Skill Libraries)
**Time:** ~75 minutes

## 学习目标

- 解释Anthropic客户端SDK（原始API）和Claude Agent SDK（线束形状）之间的区别。
- 描述子代理——并行化和上下文隔离——以及何时使用它们。
- 命名Python SDK的会话存储界面(
- 使用内置工具实现stdlib控制、独立上下文的子代理生成、生命周期钩子和会话存储。

## 这个问题

一个原始的LLM API可以实现一次往返。生产代理需要工具执行、MCP服务器、生命周期钩子、子代理生成、会话持久化、跟踪传播。Claude Agent SDK将这种形状作为库发布——与Claude Code使用的相同，为自定义代理公开。

## 这个概念

### 客户端SDK vs代理SDK

- **客户端SDK (**原始消息API。你拥有这个循环，工具和国家。
- **Agent SDK (**内置工具执行，MCP连接，钩子，子代理产卵，会话存储。Claude Code循环作为一个库。

### 内置的工具

SDK自带了10多个工具：文件读写、shell、grep、glob、web fetch等等。自定义工具通过标准工具模式接口注册。

### 子代理

Anthropic记录了两个目的：

1. * *并行化。**并行运行独立工作。“为这20个模块中的每个模块查找测试文件”是20个并行子代理任务。
2. * *上下文隔离。**子代理使用自己的上下文窗口；只有结果才会返回给协调者。管弦乐队的预算得以保留。

Python SDK最新添加：

### 会话存储

与TypeScript的协议等价性：

- Z0
- Z0
- `list_sessions()` — enumerate.
- Z0
- Z0

Z0

### 钩子

你可以注册生命周期钩子：

- Z0
- Z0
- Z0
- Z0
- Z0
- Z0

钩子是pro-workflow（第14阶段课程参考）和类似系统添加横切行为的方式。

### W3C跟踪上下文

OTel跨活动在调用者上，通过W3C跟踪上下文头传播到CLI子流程。整个多进程跟踪在后端显示为一个跟踪。

### Claude Managed Agents

托管的替代方案（beta标头）长时间运行的异步工作，内置提示缓存，内置压缩。管理基础设施的贸易管制。

### 这种模式哪里出错了

- * *代理over-spawn。**为100个小任务生成100个子代理。开销占主导地位。批处理。
- * *钩蠕变。**每个团队都会添加钩子；启动时间气球。每季度回顾一次钩子。
- * *会话膨胀。**会话累积；规模增长。使用

## 构建它

Z0

- Z0
- Z0
- Z0
- Z0
- 一个演示：主代理并行生成3个子代理（每个子代理都是隔离的），聚合结果，持久化会话。

运行该程序:

```
python3 code/main.py
```

跟踪显示子代理上下文隔离（编排程序上下文大小保持有限）、钩子执行和会话持久性。

## 使用它

- **Claude Agent SDK**用于希望Claude Code线束形状的Claude-first产品。
- **托管代理**用于托管长时间运行的异步工作。
- OpenAI agent SDK**（第16课）
- **LangGraph +自定义工具**如果你想要图形形状的状态机代替。

## 船这

Z0

## 练习

1. 添加一个子代理生成器，将20个任务分批分成5个并行子代理组。度量编排上下文大小与每个任务一个上下文大小的对比。
2. 实现a跟踪行为。
3. 电线深巢是什么样子的？
4. 把玩具移植到实物上工具注册有什么变化？
5. 阅读Claude Managed Agents文档。什么时候从自托管切换到托管？

## 关键术语

| Term | What people say | What it actually means |
|------|----------------|------------------------|
| Agent SDK | "Claude Code as a library" | Harness shape: tools, MCP, hooks, subagents, session store |
| Subagent | "Child agent" | Separate context, own budget; results bubble up |
| Session store | "Conversation DB" | Persist, load, list, delete turns with subagent cascade |
| Hook | "Lifecycle callback" | Pre/post tool, session, prompt submit, compact, stop |
| W3C trace context | "Cross-process trace" | Parent span propagates into CLI subprocess |
| Managed Agents | "Hosted harness" | Anthropic-hosted long-running async work |
| `--session-mirror` | "Transcript mirror" | Writes session turns to an external file as they stream |
| MCP server | "Tool surface" | External tool/resource source attached to the agent |

## 进一步的阅读

- Z0
- Z0
- Z0
- Z0