# 文本处理-标记化，词干化，词序化

> 语言是连续的。模型是离散的。预处理是桥梁。

**Type:** Build
**Languages:** Python
**Prerequisites:** Phase 2 · 14 (Naive Bayes)
**Time:** ~45 minutes

## 这个问题

模特看不懂“猫在跑”它读取整数。

每个NLP系统都以同样的三个问题开始。单词从哪里开始？这个词的词根是什么？我们如何对待“run”，“running”，“ran”，当它有帮助的时候是一样的，当它没有帮助的时候是不同的。

如果标记化错误，模型就会从垃圾中学习。如果你的标记器处理如果你的茎崩溃了如果你的引词器需要词性上下文，但你没有通过它，动词就会被当作名词对待。

本课从头开始构建三个预处理原语，然后展示NLTK和space如何完成相同的工作，以便您了解其中的权衡。

## 这个概念

三个操作。每个人都有自己的工作和失败模式。

**标记化**将字符串分割成标记。“令牌”故意含糊，因为正确的粒度取决于任务。经典自然语言处理的词水平。Transformer的子词。没有空格的语言的字符。

*词干截断用规则截断后缀。快速、好斗、愚蠢。`running -> run`. `organization -> organ`. 第二个是失效模式。

*词源化利用语法知识将单词还原为字典中的形式。较慢，准确，需要查找表或形态分析器。`ran -> run` (needs to know "ran" is past tense of "run"). `better -> good` (needs to know comparative forms).

经验法则。当速度很重要，你可以忍受噪音（搜索索引，粗略分类）。当意义重要的时候（问题回答，语义搜索，任何用户会读到的东西），用语序化。

## 构建它

### 步骤1：一个正则表达式单词标记器

最简单实用的标记器分割非字母数字字符，同时保留标点符号作为自己的标记。不是完美的，不是最终的，但它在一行中运行。

”“python
进口再保险

def标记(文本):
返回re.findall (r”(A-Za-z) +(?:“[A-Za-z] +) ? | | [0 - 9] + [^ \ sA-Za-z0-9]”,文本)
```

Three patterns in order of precedence. Words with optional inner apostrophe (`don't`, `it's`). Pure numbers. Any single non-whitespace non-alphanumeric character as a standalone token (punctuation).

```python
>>> tokenize("The cats weren't running at 3pm.")
['The', 'cats', "weren't", 'running', 'at', '3', 'pm', '.']
```

需要注意的故障模式。`3pm` splits to `['3', 'pm']` because we alternated between letter runs and digit runs. 对于大多数任务来说足够好了。网址、电子邮件、话题标签都坏了。对于生产，在常规模式之前添加模式。

### 第二步：波特梗（仅限步骤1a）

完整的波特算法有五个阶段的规则。步骤1a单独涵盖了最常见的英语后缀，并教授该模式。

”“python
def stem_step_1a(词):
如果word.endswith(“党卫军”):
返回单词(2):
如果word.endswith(“ies”):
返回单词(2):
如果word.endswith(“党卫军”):
返回单词
如果单词。以（"s"）和len（word）结尾> 1：
返回单词(:1)
返回单词
```

```python
>>> [stem_step_1a(w) for w in ["caresses", "ponies", "caress", "cats"]]
['caress', 'poni', 'caress', 'cat']
```

自上而下地阅读规则。The真正的波特有步骤1b可以解决这个问题。竞争规则。更早的规则获胜。秩序比任何单一的规则都重要。

### 步骤3：基于查找的归纳器

词汇化需要形态学。一个易于处理的教学版本使用一个小引理表和一个回退。

```python
LEMMA_TABLE = {
    ("running", "VERB"): "run",
    ("ran", "VERB"): "run",
    ("runs", "VERB"): "run",
    ("better", "ADJ"): "good",
    ("best", "ADJ"): "good",
    ("cats", "NOUN"): "cat",
    ("cat", "NOUN"): "cat",
    ("were", "VERB"): "be",
    ("was", "VERB"): "be",
    ("is", "VERB"): "be",
}

deflemmatize (word, pos)：
关键词=（词）低()、pos)
如果键在LEMMA_TABLE：
返回LEMMA_TABLE(例子)
if pos == "VERB" and word.endswith("ing")：
返回单词(3):
if pos == “名词” and word.endswith("s")：
返回单词(:1)
返回word.lower ()
```

```python
>>> lemmatize("running", "VERB")
'run'
>>> lemmatize("cats", "NOUN")
'cat'
>>> lemmatize("better", "ADJ")
'good'
>>> lemmatize("watched", "VERB")
'watched'
```

最后一个案例是教学的关键时刻。`watched` is not in our table and our fallback only handles `ing`. 真正的lemmatization覆盖这就是生产系统使用WordNet、spaCy的形态学分析器或完整的形态学分析器的原因。

### 第四步：用管子把它们连在一起

”“python
def preprocess(text, post_tagger =None)：
token = tokenize（text）
茎= [stem_step_1a]；Lower ()) for t in tokens]
tags = pos_tagger(tokens) if pos_tagger else [（t, “名词”）for t in tokens]
Lemmas = [lemmatize(word, pos) for word, pos in tags]
返回{“tokens“: tokens, ”茎”：茎，“lemmas”： lemmas}
```

The missing piece is a POS tagger. Phase 5 · 07 (POS Tagging) builds one. For now, default everything to `NOUN` and acknowledge the limitation.

## Use It

NLTK and spaCy ship the production versions. A few lines each.

### NLTK

```python
import nltk
nltk.download("punkt_tab")
nltk.download("wordnet")
nltk.download("averaged_perceptron_tagger_eng")

from nltk.tokenize import word_tokenize
from nltk.stem import PorterStemmer, WordNetLemmatizer
from nltk import pos_tag

text = "The cats were running."
tokens = word_tokenize(text)
stems = [PorterStemmer().stem(t) for t in tokens]
lemmatizer = WordNetLemmatizer()
tagged = pos_tag(tokens)


def nltk_pos_to_wordnet(tag):
    if tag.startswith("V"):
        return "v"
    if tag.startswith("J"):
        return "a"
    if tag.startswith("R"):
        return "r"
    return "n"


lemmas = [lemmatizer.lemmatize(t, nltk_pos_to_wordnet(tag)) for t, tag in tagged]
```

`PorterStemmer` runs all five phases. `WordNetLemmatizer` needs the POS tag translated from NLTK's Penn Treebank scheme to WordNet's abbreviation set. 上面的翻译连接是大多数教程跳过的部分。

### 宽大的

”“python
进口宽大的

NLP = space .load（"en_core_web_sm"）
doc = nlp（“猫在跑。”）

对于doc中的token：
print(令牌。文本、令牌。lemma_ token.pos_)
```

```
DET
猫猫名词
我们是AUX
跑着跑着
.        .PUNCT
```

spaCy hides the whole pipeline behind `nlp(text)`. Tokenization, POS tagging, and lemmatization all run. Faster than NLTK at scale. More accurate out of the box. The tradeoff is that you cannot easily swap individual components.

### When to pick which

| Situation | Pick |
|-----------|------|
| Teaching, research, swapping components | NLTK |
| Production, multi-language, speed matters | spaCy |
| Transformer pipeline (you'll tokenize with the model's tokenizer anyway) | Use `tokenizers` / `transformers` and skip classical preprocessing |

### The two failure modes nobody warns you about

Most tutorials teach the algorithms and stop. Two things will bite a real preprocessing pipeline, and they are almost never covered.

**Reproducibility drift.** NLTK and spaCy change tokenization and lemmatizer behavior between versions. What produced `['do', "n't"]` in spaCy 2.x may produce `["don't"]` in 3.x. Your model was trained on one distribution. Inference now runs on a different one. Accuracy quietly degrades and nobody knows why. Pin library versions in `requirements.txt`. Write a preprocessing regression test that freezes expected tokenization of 20 sample sentences. Run it on every upgrade.

**Training / inference mismatch.** Train with aggressive preprocessing (lowercase, stopword removal, stemming), deploy on raw user input, watch performance crater. This is the single most common production NLP failure. If you preprocess during training, you must run the identical function during inference. Ship preprocessing as a function inside the model package, not as a notebook cell the serving team rewrites.

## Ship It

A reusable prompt that helps engineers pick a preprocessing strategy without reading three textbooks.

Save as `outputs/prompt-preprocessing-advisor.md`:

```markdown
---
name: preprocessing-advisor
description: Recommends a tokenization, stemming, and lemmatization setup for an NLP task.
phase: 5
lesson: 01
---

You advise on classical NLP preprocessing. Given a task description, you output:

1. Tokenization choice (regex, NLTK word_tokenize, spaCy, or transformer tokenizer). Explain why.
2. Whether to stem, lemmatize, both, or neither. Explain why.
3. Specific library calls. Name the functions. Quote the POS-tag translation if NLTK is involved.
4. One failure mode the user should test for.

Refuse to recommend stemming for user-visible text. Refuse to recommend lemmatization without POS tags. Flag non-English input as needing a different pipeline.
```

## 练习

1. **容易。**扩展测试：
2. **介质。**执行波特步骤1b。如果一个单词包含元音并且以。结尾处理双辅音规则(
3. **难。**构建一个词法分析器，它使用WordNet作为查找表，但在WordNet没有条目时退回到您的波特系统。相对于普通WordNet和普通Porter测量标记语料库上的准确性。

## 关键术语

| Term | What people say | What it actually means |
|------|-----------------|-----------------------|
| Token | A word | Whatever unit the model consumes. Can be word, subword, character, or byte. |
| Stem | Root of a word | Result of rule-based suffix stripping. Not always a real word. |
| Lemma | Dictionary form | The form you'd look up. Requires grammatical context to compute correctly. |
| POS tag | Part of speech | Category like NOUN, VERB, ADJ. Needed to lemmatize accurately. |
| Morphology | Word shape rules | How a word changes form based on tense, number, case. Lemmatization depends on it. |

## 进一步的阅读

- 
- 
- 