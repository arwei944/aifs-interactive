# 向量，矩阵和运算

> 每个神经网络都是额外步骤的矩阵乘法。

**Type:** Build
**Languages:** Python, Julia
**Prerequisites:** Phase 1, Lesson 01 (Linear Algebra Intuition)
**Time:** ~60 minutes

## 学习目标

- 建立一个矩阵类与元素明智的操作，矩阵乘法，转置，行列式，和逆
- 区分元素乘法和矩阵乘法，并解释每种乘法何时适用
- 实现一个单一的密集神经网络层(
- 解释广播规则以及偏置加法在神经网络框架中的工作原理

## 这个问题

你想建立一个神经网络。阅读代码，你会看到：

```
output = activation(weights @ input + bias)
```

那TheThe如果你不知道这些操作是做什么的，这一行就很神奇了。如果你知道的话，它是在三个操作中一个层的整个向前传递。

你的模型处理的每个图像都是一个像素值矩阵。每个词嵌入都是一个向量。每个神经网络的每一层都是一个矩阵变换。如果你不熟悉矩阵运算，你就无法构建AI系统，就像你不了解变量就无法编写代码一样。

这节课教你从零开始流利地学习。

## 这个概念

### 向量：有序的数字列表

矢量是一组有方向和大小的数字。在人工智能中，向量表示数据点、特征或参数。

```
v = [3, 4]        -- a 2D vector
w = [1, 0, -2]    -- a 3D vector
```

一个二维矢量它的长度（震级）是5（3-4-5三角形）。

### 矩阵：数字网格

矩阵是一个二维网格。行和列。一个m × n矩阵有m行n列。

```
A = | 1  2  3 |     -- 2x3 matrix (2 rows, 3 columns)
    | 4  5  6 |
```

在神经网络中，权矩阵将输入向量转换为输出向量。一个有784个输入和128个输出的层使用一个128x784的权重矩阵。

### 为什么形状很重要

矩阵乘法有严格的规则：内部尺寸必须匹配。

```
(128 x 784) @ (784 x 1) = (128 x 1)
  weights       input       output

Inner dimensions: 784 = 784  -- valid
```

如果你在PyTorch中得到一个形状不匹配错误，这就是原因。

### 操作图

| Operation | What it does | Neural network use |
|-----------|-------------|-------------------|
| Addition | Element-wise combine | Adding bias to output |
| Scalar multiply | Scale every element | Learning rate * gradients |
| Matrix multiply | Transform vectors | Layer forward pass |
| Transpose | Flip rows and columns | Backpropagation |
| Determinant | Single number summary | Checking invertibility |
| Inverse | Undo a transformation | Solving linear systems |
| Identity | Do-nothing matrix | Initialization, residual connections |

### 元素乘法vs矩阵乘法

这种区别经常使初学者感到困惑。

元素方面：将匹配位置相乘。两个矩阵的形状必须相同。

```
| 1  2 |   | 5  6 |   | 5  12 |
| 3  4 | * | 7  8 | = | 21 32 |
```

矩阵乘法：行与列的点积。内部尺寸必须匹配。

```
| 1  2 |   | 5  6 |   | 1*5+2*7  1*6+2*8 |   | 19  22 |
| 3  4 | @ | 7  8 | = | 3*5+4*7  3*6+4*8 | = | 43  50 |
```

不同的操作，不同的结果，不同的规则。

### 广播

当您向输出矩阵添加偏置向量时，形状不匹配。广播扩展了较小的阵列以适应。

```
| 1  2  3 |   +   [10, 20, 30]
| 4  5  6 |

Broadcasting stretches the vector across rows:

| 1  2  3 |   | 10  20  30 |   | 11  22  33 |
| 4  5  6 | + | 10  20  30 | = | 14  25  36 |
```

每一个现代框架都自动做到这一点。当形状看似错误但代码运行时，理解它可以防止混淆。

## 构建它

### 步骤1：向量类

”“python
类向量:
Def __init__(self, data)：
自我。Data = list（Data）
自我。Size = len（self.data）

def __repr__(自我):
返回f”向量({self.data})”

Def __add__(self, other)：
返回Vector（[a + b for a, b in zip]）数据,other.data)))

Def __sub__(self, other)：
返回Vector（[a - b for a, b in zip]）数据,other.data)))

Def __mul__(self, scalar)：
return Vector（[x * scalar for x in self.data]）

Def dot(self, other)：
返回sum(a * b for a, b in zip(self。数据,other.data))

def级(自我):
返回sum(x ** 2 for x in self。数据)** 0.5
```

### Step 2: Matrix class with core operations

```python
class Matrix:
    def __init__(self, data):
        self.data = [list(row) for row in data]
        self.rows = len(self.data)
        self.cols = len(self.data[0])
        self.shape = (self.rows, self.cols)

    def __repr__(self):
        rows_str = "\n  ".join(str(row) for row in self.data)
        return f"Matrix({self.shape}):\n  {rows_str}"

    def __add__(self, other):
        return Matrix([
            [self.data[i][j] + other.data[i][j] for j in range(self.cols)]
            for i in range(self.rows)
        ])

    def __sub__(self, other):
        return Matrix([
            [self.data[i][j] - other.data[i][j] for j in range(self.cols)]
            for i in range(self.rows)
        ])

    def scalar_multiply(self, scalar):
        return Matrix([
            [self.data[i][j] * scalar for j in range(self.cols)]
            for i in range(self.rows)
        ])

    def element_wise_multiply(self, other):
        return Matrix([
            [self.data[i][j] * other.data[i][j] for j in range(self.cols)]
            for i in range(self.rows)
        ])

    def matmul(self, other):
        return Matrix([
            [
                sum(self.data[i][k] * other.data[k][j] for k in range(self.cols))
                for j in range(other.cols)
            ]
            for i in range(self.rows)
        ])

    def transpose(self):
        return Matrix([
            [self.data[j][i] for j in range(self.rows)]
            for i in range(self.cols)
        ])

    def determinant(self):
        if self.shape == (1, 1):
            return self.data[0][0]
        if self.shape == (2, 2):
            return self.data[0][0] * self.data[1][1] - self.data[0][1] * self.data[1][0]
        det = 0
        for j in range(self.cols):
            minor = Matrix([
                [self.data[i][k] for k in range(self.cols) if k != j]
                for i in range(1, self.rows)
            ])
            det += ((-1) ** j) * self.data[0][j] * minor.determinant()
        return det

    def inverse_2x2(self):
        det = self.determinant()
        if det == 0:
            raise ValueError("Matrix is singular, no inverse exists")
        return Matrix([
            [self.data[1][1] / det, -self.data[0][1] / det],
            [-self.data[1][0] / det, self.data[0][0] / det]
        ])

    @staticmethod
    def identity(n):
        return Matrix([
            [1 if i == j else 0 for j in range(n)]
            for i in range(n)
        ])
```

### 第三步：看看效果如何

”“python
A = Matrix（[[1,2], [3,4]]）
B =矩阵（[[5,6],[7,8]]）

print("A + B =", (A + B).data)
print("A @ B =", A.matmul(B).data)
print("A^T =", A.transpose().data)
print("det(A) =", A.determinant())
print("A^-1 =", A.inverse_2x2().data)

I = Matrix.identity(2)
打印(“@ ^ 1 = ",A.matmul (A.inverse_2x2 ()) . data)
```

### Step 4: Connect to neural networks

```python
import random

inputs = Matrix([[0.5], [0.8], [0.2]])
weights = Matrix([
    [random.uniform(-1, 1) for _ in range(3)]
    for _ in range(2)
])
bias = Matrix([[0.1], [0.1]])

def relu_matrix(m):
    return Matrix([[max(0, val) for val in row] for row in m.data])

pre_activation = weights.matmul(inputs) + bias
output = relu_matrix(pre_activation)

print(f"Input shape: {inputs.shape}")
print(f"Weight shape: {weights.shape}")
print(f"Output shape: {output.shape}")
print(f"Output: {output.data}")
```

这是一个单一的致密层：每个神经网络中的每个密集层都是这样做的。

## 使用它

NumPy以更少的行数和更快的数量级完成上述所有工作。

”“python
导入numpy为np

A = np。Array （[[1,2], [3,4]]）
B = np。Array （[[5,6], [7,8]]）

print("A + B =\n", A + B)
print("A * B (element-wise) =\n", A * B)
print("A @ B (matrix multiply) =\n", A @ B)
print("A^T =\n", A.T)
print("det(A) =", np.linalg.det(A))
print("A^-1 =\n", np.linalg.inv(A))
print("I =\n", np.eye(2))

输入= np.random。randn (3,1)
权重= np.random。randn(2、3)
Bias = np.array（[[0.1], [0.1]]）
输出= np。最大值（0，权重@输入+偏置）

神经网络层：{权重。形状}@{输入。Shape} = {output.shape}")
打印(f“输出:\ n{输出}”)
```

The `@` operator in Python calls `__matmul__`. NumPy implements it with optimized BLAS routines written in C and Fortran. Same math, 100x faster.

Broadcasting in NumPy:

```python
matrix = np.array([[1, 2, 3], [4, 5, 6]])
bias = np.array([10, 20, 30])
print(matrix + bias)
```

NumPy自动跨两行广播1D偏差。这就是偏置加法在每个神经网络框架中的工作原理。

## 船这

这节课提出了通过几何直觉来教授矩阵运算的提示。参见

这里构建的矩阵类是我们在第3阶段第10课中构建的迷你神经网络框架的基础。

## 练习

1. **验证逆。**乘法用三个不同的2x2矩阵试试。当行列式为零时会发生什么？

2. **实现3x3逆。**扩展Matrix类，使用adjugate方法计算3x3矩阵的逆。对NumPy进行测试

3. **搭建两层网络。**只使用你的Matrix类（没有NumPy），创建一个两层神经网络：输入(3)->隐藏(4)->输出(2)。初始化随机权重，运行向前传递，并验证所有形状是否正确。

## 关键术语

| Term | What people say | What it actually means |
|------|----------------|----------------------|
| Vector | "An arrow" | An ordered list of numbers. In AI: a point in high-dimensional space. |
| Matrix | "A table of numbers" | A linear transformation. It maps vectors from one space to another. |
| Matrix multiply | "Just multiply the numbers" | Dot products between every row of the first matrix and every column of the second. Order matters. |
| Transpose | "Flip it" | Swap rows and columns. Turns an m x n matrix into n x m. Critical in backpropagation. |
| Determinant | "Some number from the matrix" | Measures how much the matrix scales area (2D) or volume (3D). Zero means the transformation crushes a dimension. |
| Inverse | "Undo the matrix" | The matrix that reverses the transformation. Only exists when the determinant is not zero. |
| Identity matrix | "The boring matrix" | The matrix equivalent of multiplying by 1. Used in residual connections (ResNets). |
| Broadcasting | "Magic shape fixing" | Stretching a smaller array to match a larger one by repeating along missing dimensions. |
| Element-wise | "Regular multiplication" | Multiply matching positions. Both arrays must have the same shape (or be broadcastable). |

## 进一步的阅读

- [3Blue1Brown: Essence of Linear Algebra](https://www.3blue1brown.com/topics/linear-algebra) -这里涵盖的每个操作的视觉直觉
- [NumPy关于广播的文档](https://numpy.org/doc/stable/user/basics.broadcasting.html) - NumPy遵循的确切规则
- [斯坦福CS229线性代数评论](http://cs229.stanford.edu/section/cs229-linalg.pdf) - ml特定线性代数的简明参考