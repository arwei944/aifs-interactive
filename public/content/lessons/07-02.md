# Self-attention starting from scratch

> Attention is a query table, and every word asks, "Who is important to me?" -- Then study the answers.

**Type:** Build
**Languages:** Python
**Prerequisites:** Phase 3 (Deep Learning Core), Phase 5 Lesson 10 (Sequence-to-Sequence)
**Time:** ~90 minutes

## Learning objectives

- Implement scaled dot product self-attention from scratch using only NumPy, including query/key/value projection and softmax weighted sum
- Establish a multi-head attention layer that can split heads, calculate parallel attention, and concatenate the results
- How does the tracking attention matrix capture the label relationship and explain why scaling by sqrt (d_k) prevents softmax saturation
- Apply causal masking to convert bidirectional attention into autoregressive (decoded) attention

## This question

An rnn processes one token sequence at a time. When you reach token 50, the information from token 1 has been compressed through 50 compression steps. Remote dependencies are compressed into a fixed-size hidden state - this is a bottleneck that no LSTM control can completely solve.

The Bahdanau Focus paper in 2014 provided a solution: having the decoder review each encoder position and determine which one is important for the current step. But it is still fixed on an RNN. The 2017 paper "Attention is All You Need" raised a more acute question: What if attention is the "only" mechanism? There was no recurrence. There is no convolution. Just pay attention.

Self-attention enables each position in the sequence to notice other positions within a single parallel step. This is what makes Transformers fast, scalable and dominant.

## This concept

### Database search analogy

View attention as a soft database search:

```
Traditional database:
  Query: "capital of France"  -->  exact match  -->  "Paris"

Attention:
  Query: "capital of France"  -->  similarity to ALL keys  -->  weighted blend of ALL values
```

Each token generates three vectors:
- ** Query (Q)** : "What am I looking for?"
- Key (K) : "What do I contain?"
- **Value (V)** : "If selected, what information do I need to provide?"

The dot product between the query and all keys generates the attention score. A high score means "This key matches my query." These scores are weighted on value. The output is the weighted sum of the values.

### The calculation of Q K V

Each tag embedding is projected through three learned weight matrices:

```
Input embeddings (sequence of n tokens, each d-dimensional):

  X = [x1, x2, x3, ..., xn]       shape: (n, d)

Three weight matrices:

  Wq  shape: (d, dk)
  Wk  shape: (d, dk)
  Wv  shape: (d, dv)

Projections:

  Q = X @ Wq    shape: (n, dk)      each token's query
  K = X @ Wk    shape: (n, dk)      each token's key
  V = X @ Wv    shape: (n, dv)      each token's value
```

Visually, for a token:

```
             Wq
  x_i ------[*]------> q_i    "What am I looking for?"
       |
       |     Wk
       +----[*]------> k_i    "What do I contain?"
       |
       |     Wv
       +----[*]------> v_i    "What do I offer?"
```

### Attention matrix

Once you have obtained the Q, K, V of all the labels, the attention score will form a matrix:

```
Scores = Q @ K^T    shape: (n, n)

              k1    k2    k3    k4    k5
        +-----+-----+-----+-----+-----+
   q1   | 2.1 | 0.3 | 0.1 | 0.8 | 0.2 |   <- how much q1 attends to each key
        +-----+-----+-----+-----+-----+
   q2   | 0.4 | 1.9 | 0.7 | 0.1 | 0.3 |
        +-----+-----+-----+-----+-----+
   q3   | 0.2 | 0.6 | 2.3 | 0.5 | 0.1 |
        +-----+-----+-----+-----+-----+
   q4   | 0.9 | 0.1 | 0.4 | 1.7 | 0.6 |
        +-----+-----+-----+-----+-----+
   q5   | 0.1 | 0.3 | 0.2 | 0.5 | 2.0 |
        +-----+-----+-----+-----+-----+

Each row: one token's attention over the entire sequence
```

### Why scale?

The dot product increases with the dimension dk. If dk = 64, the dot product can be within the range of 10, pushing softmax to the region where the gradient disappears. Solution: Divide by the square root (dk).

```
Scaled scores = (Q @ K^T) / sqrt(dk)
```

This keeps the value within the range where softmax generates useful gradients.

### Softmax converts scores into weights

Softmax converts the original score into a probability distribution for each row:

```
Raw scores for q1:   [2.1, 0.3, 0.1, 0.8, 0.2]
                            |
                         softmax
                            |
Attention weights:   [0.52, 0.09, 0.07, 0.14, 0.08]   (sums to ~1.0)
```

Now each token has a set of weights, indicating the degree of attention paid to other tokens.

### Weighted values and

The final output of each tag is the weighted sum of all value vectors:

```
output_i = sum( attention_weight[i][j] * v_j  for all j )

For token 1:
  output_1 = 0.52 * v1 + 0.09 * v2 + 0.07 * v3 + 0.14 * v4 + 0.08 * v5
```

### Complete pipeline

```
                    +-------+
  X (input)  ----->|  @ Wq  |-----> Q
                    +-------+
                    +-------+
  X (input)  ----->|  @ Wk  |-----> K
                    +-------+                     +----------+
                    +-------+                     |          |
  X (input)  ----->|  @ Wv  |-----> V ---------->| weighted |----> output
                    +-------+          ^          |   sum    |
                                       |          +----------+
                              +--------+--------+
                              |    softmax      |
                              +---------+-------+
                                        ^
                              +---------+-------+
                              | Q @ K^T / sqrt  |
                              +-----------------+
```

One line of formula:

```
Attention(Q, K, V) = softmax( Q @ K^T / sqrt(dk) ) @ V
```

:::viz type="attention"
通过交互式可视化理解 Self-Attention：切换到 Q/K/V 计算流程 Tab 观察矩阵运算步骤，或切换到 Multi-Head Tab 观察并行注意力模式。
:::

## Build it

### Step 1: Start Softmax from scratch

Softmax converts the original logic into probability. Subtract the maximum value of numerical stability.

”“python
Import numpy as np

Def softmax (x)：
= x - np。max（x, axis=-1, keepdim =True）
Exp_x = np.exp
exp / np：sum（exp_x, axis=-1, keepdim =True）

Logic = np.array ([2.0,1.0,0.1])
Print (f "logits: {logits}")
Print (f "softmax: {softmax (logits)}")
Print (f "sum: {softmax (logits).sum (): 4f}")
```

### Step 2: Scaled dot-product attention

The core function. Takes Q, K, V matrices and returns the attention output plus the weight matrix.

```python
def scaled_dot_product_attention(Q, K, V):
    dk = Q.shape[-1]
    scores = Q @ K.T / np.sqrt(dk)
    weights = softmax(scores)
    output = weights @ V
    return output, weights
```

### Step 3: Learn the projected self-attention course

A complete self-focus module with Wq, Wk, and Wv weight matrices, initialized to Xavier like scaling.

”“python
类SelfAttention:
Def __init__(self, d_model, dk, dv, seed=42)：
RNG = np.random.default_rng
Scale = np.sqrt（2.0 / (d_model + dk)）
自我。Wq = rng。Normal （0, scale, (d_model, dk)）
自我。Wk = rng。Normal （0, scale, (d_model, dk)）
Scale_v = np.sqrt（2.0 / (d_model + dv)）
自我。Wv = rng。Normal （0, scale_v, (d_model, dv)）
自我。Dk = Dk

defforward (self, X) :
Q = x@self. Wq
K = x@self. weeks
V = x@self. West Virginia
Output, weight = scaled_dot_product_attention (Q, K, V)
Return the output, weight
```

### Step 4: Run it on a sentence

Create fake embeddings for a sentence and watch the attention weights.

```python
sentence = ["The", "cat", "sat", "on", "the", "mat"]
n_tokens = len(sentence)
d_model = 8
dk = 4
dv = 4

rng = np.random.default_rng(42)
X = rng.normal(0, 1, (n_tokens, d_model))

attn = SelfAttention(d_model, dk, dv, seed=42)
output, weights = attn.forward(X)

print("Attention weights (each row: where that token looks):\n")
print(f"{'':>6}", end="")
for token in sentence:
    print(f"{token:>6}", end="")
print()

for i, token in enumerate(sentence):
    print(f"{token:>6}", end="")
    for j in range(n_tokens):
        w = weights[i][j]
        print(f"{w:6.3f}", end="")
    print()
```

### Step 5: Visualize attention using ASCII heat maps

Map the attention weights onto the characters for rapid visualization.

”“python
Def ascii_heatmap (weights, notation, character = "████") :
N = len (tokens)
Print (f "\ n{" :> 6}", end = "")
For t in tokens:
Print (f "{t: > 6}", end = "")
print ()

For I within the range of (n) :
Print (f "{token (me):> 6}", end = "")
For j in the range (n) :
Level = int(weights[i][j] * (len (chars) - 1)/weights.max())
Level = min (Level, len(chars) - 1)
Print (f"{" + chars[level] + "}", end="")
print ()

ascii_heatmap(Weight, Sentence
```

## Use It

PyTorch's `nn.MultiheadAttention` does exactly what we built, plus multi-head splitting and output projection:

```python
import torch
import torch.nn as nn

d_model = 8
n_heads = 2
seq_len = 6

mha = nn.MultiheadAttention(embed_dim=d_model, num_heads=n_heads, batch_first=True)

X_torch = torch.randn(1, seq_len, d_model)

output, attn_weights = mha(X_torch, X_torch, X_torch)

print(f"Input shape:            {X_torch.shape}")
print(f"Output shape:           {output.shape}")
print(f"Attention weight shape: {attn_weights.shape}")
print(f"\nAttn weights (averaged over heads):")
print(attn_weights[0].detach().numpy().round(3))
```

The key difference is: Multi-head attention runs multiple attention functions in parallel, each with its own Q, K, V projections of size dk = d_model/n_heads, and then concatenates the results. This enables the model to handle different types of relationships simultaneously.

## This ship

This lesson generates:
- 

## Practice

1. "Modify
2. Start from scratch to achieve multi-head focus: Split Q, K, and V into
3. Take two sentences of the same length and compare their attention patterns through the same self-attention instance. What changes are there? What remains unchanged?

## Key terms

| Term | What people say | What it actually means |
|------|----------------|----------------------|
| Query (Q) | "The question vector" | A learned projection of the input that represents what information this token is looking for |
| Key (K) | "The label vector" | A learned projection that represents what information this token contains, matched against queries |
| Value (V) | "The content vector" | A learned projection carrying the actual information that gets aggregated based on attention scores |
| Scaled dot-product attention | "The attention formula" | softmax(QK^T / sqrt(dk)) @ V - scaling prevents softmax saturation in high dimensions |
| Self-attention | "The token looks at itself and others" | Attention where Q, K, V all come from the same sequence, letting every position attend to every other position |
| Attention weights | "How much focus" | A probability distribution over positions, produced by softmax over scaled dot products |
| Multi-head attention | "Parallel attention" | Running multiple attention functions with different projections, then concatenating results for richer representations |

## Further reading

- 
- 
- 