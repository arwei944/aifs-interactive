# 从头开始建造一个Transformer-顶点

> 13课。一个模型。没有捷径。

**Type:** Build
**Languages:** Python
**Prerequisites:** Phase 7 · 01 through 13. Don't skip.
**Time:** ~120 minutes

## 这个问题

你读过所有的报纸。你已经实现了注意力，多头分割，位置编码，编码器和解码器块，BERT和GPT损失，MoE， KV缓存。现在让他们一起完成一项真正的任务。

顶点：在字符级语言建模任务上端到端训练一个小型的仅解码器转换器。上面写着莎士比亚。它产生了新的莎士比亚。它足够小，可以在笔记本电脑上训练不到10分钟。交换更大的数据集和更长的训练时间可以得到一个真正的LM，这是足够正确的。

这是本课程的“nanoGPT”。它不是原创的——Karpathy的2023 nanoGPT教程是每个学生至少写一次的参考实现。我们抬起形状并围绕我们所覆盖的内容重新配置它。

## 这个概念


架构，注释如下：

```
input tokens (B, N)
   │
   ▼
token embedding + positional embedding  ◀── Lesson 04 (RoPE option)
   │
   ▼
┌──── block × L ────────────────────┐
│  RMSNorm                          │  ◀── Lesson 05
│  MultiHeadAttention (causal)      │  ◀── Lesson 03 + 07 (causal mask)
│  residual                         │
│  RMSNorm                          │
│  SwiGLU FFN                       │  ◀── Lesson 05
│  residual                         │
└────────────────────────────────── ┘
   │
   ▼
final RMSNorm
   │
   ▼
lm_head (tied to token embedding)
   │
   ▼
logits (B, N, V)
   │
   ▼
shift-by-one cross-entropy            ◀── Lesson 07
```

### 我们的产品

- 
- 
- 
- 
- 
- 训练循环与AdamW，余弦LR，梯度裁剪。
- 莎士比亚文本的字符级标记器。

### 我们没有发布的东西

- 绳-在第04课概念上实现。为了简单起见，这里我们使用学习的位置嵌入。练习要求你把绳子换成绳子。
- 千伏缓存在生成过程中-每一代步骤重新计算对整个前缀的关注。更慢，但更简单。练习要求您添加KV缓存。
- Flash注意- PyTorch 2.0+在输入匹配时自动调度；我们使用
- MoE -每个块的单个FFN。你们在第11课见过MoE。

### 目标指标

在Mac M2笔记本电脑上，一个4层4头d_model=128 GPT训练了2000步

- 训练损失在约6分钟内从~4.2（随机）收敛到~1.5。
- 采样后的输出看起来像莎士比亚：出现了古旧的单词、换行符、像“ROMEO”这样的专有名称。
- 价值损失（保留文本的最后10%）与训练损失密切相关；在这个尺寸/预算下没有过度拟合。

## 构建它

本课使用PyTorch。安装参见脚本处理：

- 下载
- 字节级字符标记器。
- 火车/val以90/10分割。
- 在支持的硬件上使用bf16自动播放的训练循环。
- 训练完成后采样。

### 第一步：数据

”“python
Text = open(" tinysshakespeare .txt").read（）
Chars = sorted（set(text)）
Stoi = {c: I for I, c in enumerate（字符）}
Itos = {i: c for c, i in stoi.items()}
编码= lambda s: [stoi[c] for c in s]
Decode = lambda xs: “”。Join （itos[x] for x in xs）
```

65 unique characters. Tiny vocabulary. Fits a 4-byte vocab_size. No BPE, no tokenizer drama.

### Step 2: model

See `code/main.py`. The block is textbook from Lesson 05 — pre-norm, RMSNorm, SwiGLU, causal MHA. Parameter count for 4/4/128: ~800K.

### Step 3: training loop

Get a random batch of length-256 token windows. Forward. Shift-by-one cross-entropy. Backward. AdamW step. Log. Repeat.

```python
for step in range(max_steps):
    x, y = get_batch("train")
    logits = model(x)
    loss = F.cross_entropy(logits.view(-1, vocab_size), y.view(-1))
    loss.backward()
    torch.nn.utils.clip_grad_norm_(model.parameters(), 1.0)
    opt.step()
    opt.zero_grad()
```

### 第四步：取样

给出提示，重复向前，从top-p逻辑中取样，追加，然后继续。在500个令牌后停止。

### 步骤5：读取输出

2000步后：

```
ROMEO:
Away and mild will not thy friend, that thou shalt wit:
The chief that well shame and hath been his friends,
...
```

不是莎士比亚。但Shakespeare-shaped。在笔记本电脑上使用约800K个参数和6分钟即可获得明显的胜利。

## 使用它

这个顶点是一个参考架构。三个扩展将它运送到真正的东西：

1. **交换标记器。**使用BPE(例如：词汇大小从65个跳跃到~50,000个。模型容量需要扩大以进行补偿。
2. **在更大的语料库上训练。**使用对于一个125m参数的GPT，单个A100上的10B个令牌需要大约24小时。
3. **增加绳索+ KV缓存+闪光注意。下面的练习将引导你完成每一项。

最终得到一个125m参数的GPT，生成流利的英语。不是前沿模型。但同样的代码路径——只是更大——是Karpathy、EleutherAI和艾伦研究所在2026年用来训练研究检查点的。

## 船这

参见该技能回顾了一个从头开始的转换器实现，以检查前面所有13课的正确性。

## 练习

1. **容易。**运行验证训练模型的最后一步验证损失低于2.0。改变
2. **介质。**用RoPE替换学习到的位置嵌入。将旋转应用到Q和K里面训练和验证价值损失至少同样低。
3. **介质。**在采样环路中实现KV缓存。生成500个带和不带缓存的令牌。笔记本电脑上的挂钟应该能提高5 - 20倍。
4. **难。**添加第二个头部到预测下一个加一个令牌的模型（MTP -来自DeepSeek-V3的多令牌预测）。共同训练。有帮助吗？
5. **难。**将单个FFN替换为4专家MoE。路由器+ top-2路由。查看val损失在匹配的活动参数处是如何变化的。

## 关键术语

| Term | What people say | What it actually means |
|------|-----------------|-----------------------|
| nanoGPT | "Karpathy's tutorial repo" | Minimal decoder-only transformer training code, ~300 LOC; the canonical reference. |
| tinyshakespeare | "The standard toy corpus" | ~1.1 MB of text; every character-LM tutorial since 2015 uses it. |
| Tied embeddings | "Share input/output matrix" | LM head weight = transpose of token embedding matrix; saves parameters, improves quality. |
| bf16 autocast | "Training precision trick" | Run forward/back in bf16, keep optimizer state in fp32; standard since 2021. |
| Gradient clipping | "Stops spikes" | Cap global grad norm at 1.0; prevents training blowups. |
| Cosine LR schedule | "The 2020+ default" | LR ramps up linearly (warmup) then decays cosine-shaped to 10% of peak. |
| MFU | "Model FLOP Utilization" | Achieved FLOPs / theoretical peak; 40% dense, 30% MoE is strong in 2026. |
| Val loss | "Held-out loss" | Cross-entropy on data the model never saw; overfit detector. |

## 进一步的阅读

- 