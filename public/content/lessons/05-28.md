# Long-term situation assessment - NIAH, ruler, long workbench, MRCR

> Gemini 3 Pro promoted 10 million context tokens. When there were 1 million tokens, the 8-dose MRCR dropped to 26.3%. Advertising does not equal availability. Long context evaluation tells you the actual capacity of the delivered model.

**Type:** Learn
**Languages:** Python
**Prerequisites:** Phase 5 · 13 (Question Answering), Phase 5 · 23 (Chunking Strategies)
**Time:** ~60 minutes

## This question

You have a 200-page contract. This model declares a 1m token context. You paste the contract in and then ask, "What is a termination clause?" The model provides the answer - but the answer comes from the cover, as the termination clause lies deep within 120,000 tokens, beyond where the model actually participates.

This is the situation in 2026 - the ability gap. The specification sheet states 1M or 10M. The reality is that 60-70% of them are available, and "available" depends on the task.

- ** Retrieval (Single needle in the haystack) : ** Nearly perfect, reaching the maximum advertising value of the cutting-edge model.
- ** Multi-hop/aggregation ** : It drops sharply when exceeding ~128k on most models.
- Reasoning about scattered facts: ** The first failed task.

Long-term situation assessment measures these axes. This lesson lists benchmarks, what each benchmark actually measures, and how to build custom pointer tests for your domain.

## This concept


*Looking for a needle in a haystack (NIAH, 2023). Put a fact (" This magical word is pineapple ") in a long context with controlled depth. The model is required to retrieve it. Scanning depth × length. The original long context benchmark. The frontier models are now saturated. This is a necessary but insufficient baseline.

*RULER (Nvidia, to be launched in 2024). 4 categories and 13 types of tasks: retrieval (single/multi-key/multi-value), multi-hop tracking (variable tracking), aggregation (common word frequencies), QA. Configurable context length (4k to 128k+). A model that saturated NIAH but failed on multiple jumps was revealed. In the version released in 2024, only half of the 17 models claiming a 32k+ environment maintained the quality of 32k.

**LongBench v2 (2024). 503 multiple-choice questions, 8k-2M word context, 6 task categories: single-document QA, multi-document QA, long context learning, long dialogue, code repurchase, long structured data. The production benchmark of actual long context behavior.

**MRCR (Multi-Round Coreference Resolution). Large-scale multi-circle co-reference. 8 doses, 24 doses, 100 doses. It reveals how many facts a model can handle before attention deteriorates.

*NoLiMa. "Non-vocabulary needle." " There is no text overlap between the pointer and the query. Retrieval requires one step of semantic reasoning. It's more difficult than NIAH.

*"Helmet. Connect many files and ask questions to anyone. Test selective attention.

*"BABILong. Embed bAbI inference chains in unrelated haystacks. Test "looking for a needle in a haystack" reasoning, not just retrieval.

### What is actually reported?

- "Advertising context window." Specification serial number.
- Effective retrieval length. NIAH passes certain thresholds (for example, 90%).
- Effective reasoning length. Multi-hop or aggregation passes through this threshold.
- Degradation curve. Accuracy and context length, drawn by task type.

Your specification sheet has two numbers: retrieval valid and inference valid. Usually, the reasoning effect is 25-50% of that of the advertising window.

## Build it

### Step 1: Customize the NIAH for your domain

See skeleton

”“python
Def build_haystack(filler_text, needle, depth_ratio, total_tokens) :
If not (0.0 <= depth_ratio <= 1.0) :
Raise a ValueError (f"depth_ratio must be within [0,1] to obtain {depth_ratio}")
If total_tokens <= 0:
Raise a ValueError (f"total_tokens must be positive, got {total_token}")

filler_token = tokenize (filler_text)
Needle_tokens = tokenize (needle)
If it is not filler_tokens:
Throw a ValueError ("filler_text did not generate a token ")

Repeat filling until there is sufficient time to fill the main body of the haystack.
Body_len = max (total_tokens - len(needle_tokens), 0)
When len(filler_tokens) < body_len:
filler_token = filler_token + filler_token
filler_token = filler_token [:body_len]

    insert_at = min(int(body_len * depth_ratio), body_len)
    haystack = filler_tokens[:insert_at] + needle_tokens + filler_tokens[insert_at:]
    return " ".join(haystack)


Def score_niah (model, haystack, problem, expectation) :
answer = model.plete (f"Context: {haystack}\nQ: {question}\nA:", max_tokens=50)
If answer.lower () is expected to return 1, otherwise return 0
```

Sweep `depth_ratio` ∈ {0, 0.25, 0.5, 0.75, 1.0} × `total_tokens` ∈ {1k, 4k, 16k, 64k}. Plot the heatmap. That is the NIAH card for your target model.

### Step 2: a multi-needle variant

```python
def build_multi_needle(filler, needles, total_tokens):
    depths = [0.1, 0.4, 0.7]
    chunks = [filler[:int(total_tokens * 0.1)]]
    for depth, needle in zip(depths, needles):
        chunks.append(needle)
        next_chunk = filler[int(total_tokens * depth): int(total_tokens * (depth + 0.3))]
        chunks.append(next_chunk)
    return " ".join(chunks)
```

Like "What are those three magical words?" Such a question requires retrieving all three words. The success of a single injection does not predict the success of multiple injections.

### Step 3: Multi-hop Variable tracking (rule-style)

”“python
haystack = """X1 = 42. ... (Filler)... X2 = x1 + 10. ... (Filler)... X3 = x2 * 2."“”
Question: "What is X3?"
```

The answer requires chaining three assignments. Frontier models at 128k often drop to 50-70% accuracy here.

### Step 4: LongBench v2 on your stack

```python
from datasets import load_dataset
longbench = load_dataset("THUDM/LongBench-v2")

def eval_model_on_longbench(model, subset="single-doc-qa"):
    tasks = [x for x in longbench["test"] if x["task"] == subset]
    correct = 0
    for x in tasks:
        answer = model.complete(x["context"] + "\n\nQ: " + x["question"], max_tokens=20)
        if normalize(answer) == normalize(x["answer"]):
            correct += 1
    return correct / len(tasks)
```

Report the accuracy of each category. The total score masks the huge differences at the task level.

## Trap

- "NIAH-only evaluation." Passing NIAH with 1M tokens does not indicate multiple hops. Always run RULER or custom multi-hop tests.
- Uniform depth sampling. Many implementations only test a depth of 0.5. The test depth =0,0.25,0.5,0.75,1.0 - the effect of "getting lost in the middle" is real.
- Overlaps with the vocabulary of fillers. If the needle and the filler share keywords, the retrieval becomes insignificant. Use nolima style non-overlapping needles.
- Ignore the delay. m-token prompts that it takes 30 to 120 seconds to pre-fill. The time and accuracy of measuring the first mark.
- ** Vendor-self-reported number. OpenAI, Google, and Anthropic have all released their own scores. Always re-run independently on your use cases.

## Use it

Stack for 2026

| Situation | Benchmark |
|-----------|-----------|
| Quick sanity check | Custom NIAH at 3 depths × 3 lengths |
| Model selection for production | RULER (13 tasks) at your target length |
| Real-world QA quality | LongBench v2 single-doc-QA subset |
| Multi-hop reasoning | BABILong or custom variable-tracing |
| Conversational / dialogue | MRCR 8-needle at your target length |
| Model upgrade regression | Fixed in-house NIAH + RULER harness, run on every new model |

A rule of thumb for production: Never trust the context window until the NIAH + 1 inference task reaches its expected length.

## This ship

Save as

"On sale
---
Name :long-context-eval
Description: Design a long context evaluation unit for the given model and use case.
Version :1.0.0
Phase :5
Lesson :28
Tags: [nlp, Long Context, Evaluation]
---

Given the target model, target context length and use cases, output:

1. Test. NIAH depth × length grid; The ruler's seed Custom domain task.
2. Sampling. The depth is 0,0.25,0.5,0.75,1.0 at each length.
3. Indicator. Retrieval pass rate Reasoning pass rate time-to-first-token; cost-per-query.
4. Deadline. Effective retrieval length (90% pass) and effective inference length (70% pass). Two reports.
5. Return. The fixed seat belt will run again each time it is upgraded, with a surface delta.

Refuse to trust the context window from the model card alone. Refuse to conduct niah evaluations only for any multi-hop workloads. Refuse the long-term background score self-reported by the supplier as independent evidence.
```

## Exercises

1. **Easy.** Build a NIAH with 3 depths (0.25, 0.5, 0.75) × 3 lengths (1k, 4k, 16k). Run on any model. Plot pass rate as a 3×3 heatmap.
2. **Medium.** Add a 3-needle variant. Measure retrieval of all 3 at each length. Compare to single-needle pass rate at the same length.
3. **Hard.** Construct a variable-tracing task (X1 → X2 → X3, with 3 hops) embedded in 64k of filler. Measure accuracy across 3 frontier models. Report effective reasoning length per model.

## Key Terms

| Term | What people say | What it actually means |
|------|-----------------|-----------------------|
| NIAH | Needle in haystack | Plant a fact in filler, ask the model to retrieve it. |
| RULER | NIAH on steroids | 13 task types across retrieval / multi-hop / aggregation / QA. |
| Effective context | The real capacity | Length at which accuracy still holds above threshold. |
| Lost in the middle | Depth bias | Models under-attend to content in the middle of long inputs. |
| Multi-needle | Many facts at once | Multiple plants; tests attention juggling, not retrieval alone. |
| MRCR | Multi-round coref | 8, 24, or 100-needle coreference; exposes attention saturation. |
| NoLiMa | Non-lexical needle | Needle and query share no literal tokens; requires reasoning. |

## Further Reading

- [Kamradt (2023). Needle in a Haystack analysis](https://github.com/gkamradt/LLMTest_NeedleInAHaystack) — the original NIAH repo.
- [Hsieh et al. (2024). RULER: What's the Real Context Size of Your Long-Context LMs?](https://arxiv.org/abs/2404.06654) — the multi-task benchmark.
- [Bai et al. (2024). LongBench v2](https://arxiv.org/abs/2412.15204) — real-world long-context eval.
- [Modarressi et al. (2024). NoLiMa: Non-lexical needles](https://arxiv.org/abs/2404.06666) — harder needles.
- [Kuratov et al. (2024). BABILong](https://arxiv.org/abs/2406.10149) — reasoning-in-haystack.
- [Liu et al. (2024). Lost in the Middle: How Language Models Use Long Contexts](https://arxiv.org/abs/2307.03172) — the depth-bias paper.