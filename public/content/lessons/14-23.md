# openelementetry GenAI语义约定

> OpenTelemetry的GenAI SIG（于2024年4月推出）定义了代理遥测的标准模式。Span名称、属性和内容捕获规则在不同的供应商之间趋同，因此代理跟踪在Datadog、Grafana、Jaeger和Honeycomb中意味着相同的东西。

**Type:** Learn + Build
**Languages:** Python (stdlib)
**Prerequisites:** Phase 14 · 13 (LangGraph), Phase 14 · 24 (Observability Platforms)
**Time:** ~60 minutes

## 学习目标

- 命名GenAI跨度类别：模型/客户端、代理、工具。
- 区分
- 列出顶级GenAI属性：提供者名称、请求模型、数据源ID。
- 解释内容捕获合同：选择加入；

## 这个问题

每个供应商都发明了自己的span名称。运维团队最终构建每个框架的仪表板。OpenTelemetry的GenAI SIG通过定义一个整个生态系统目标的标准来解决这个问题。

## 这个概念

### 跨类别

1. **模型/客户端跨度。**覆盖原始LLM呼叫。由提供商sdk （Anthropic, OpenAI, Bedrock）和框架模型适配器发出。
2. * *代理跨越。**
3. * *工具跨越。**每个工具调用一个；通过父子关系连接到代理范围。

### 代理范围命名

- Span名称：退回到
- 跨度:
  - **CLIENT** -用于远程代理服务（OpenAI助手API， Bedrock Agents）。
  - **INTERNAL** -用于进程内代理框架（LangChain, CrewAI, local ReAct）。

### 关键属性

- Z0
- Z0
- Z0
- Z0
- Z0
- Z0

针对Anthropic、Azure AI Inference、AWS Bedrock、OpenAI等技术的特定约定已经存在。

### 内容捕捉

默认规则：默认情况下，仪表不应该捕获输入/输出。捕获可通过以下方式选择：

- Z0
- Z0
- Z0

推荐的生产模式：在外部存储内容（S3，您的日志存储），在跨度上记录引用（指针id，而不是散文）。这是第27课内容中毒防御与可观察性联系在一起。

### 稳定

截至2026年3月，大多数公约都是实验性的。选择加入稳定预览：

```
OTEL_SEMCONV_STABILITY_OPT_IN=gen_ai_latest_experimental
```

Datadog v1.37+将GenAI属性本地映射到它的LLM Observability模式中。其他后端（Grafana, Honeycomb, Jaeger）支持原始属性。

### 这种模式哪里出错了

- **捕获完整的提示在跨度。**操作人员可以读取的PII、机密、客户数据。在外部存储。
- **否**当丢失归因时，多提供商仪表板会损坏。
- **没有父链接的跨度。**孤立的工具跨度。始终传播上下文。
- **未设置稳定性选项。**你的属性可能会被重命名后端升级。

## 构建它

Z0

- Z0
- Z0
- 脚本代理运行，发出：
- 一种内容捕获模式，在外部存储提示并记录span上的id。

运行该程序:

```
python3 code/main.py
```

输出：一个包含所有必需的GenAI属性的跨度树，以及一个显示可选内容引用的“外部存储”。

## 使用它

- **Datadog LLM Observability** (v1.37+)原生映射属性。
- **Langfuse / Phoenix / Opik**（第24课）-自动仪器化生态系统。
- **Jaeger / Honeycomb / Grafana Tempo** - OTel的原始痕迹；根据GenAI属性构建仪表板。
- **自托管** -运行OTel收集器与GenAI处理器。

## 船这

Z0

## 练习

1. 用仪器仪表你的第01课ReAct循环发送到Jaeger实例。
2. 以“仅引用”模式添加内容捕获：提示SQLite， span属性仅携带行id。
3. 阅读关于把它连接到你的第09课记忆搜索中。
4. 集合
5. 构建一个仪表板：仅从GenAI属性“哪些工具错误与哪些模型相关”。

## 关键术语

| Term | What people say | What it actually means |
|------|----------------|------------------------|
| GenAI SIG | "OpenTelemetry GenAI group" | OTel working group defining the schema |
| invoke_agent | "Agent span" | Name of the span representing an agent run |
| CLIENT span | "Remote call" | Span for a call to a remote agent service |
| INTERNAL span | "In-process" | Span for an in-process agent run |
| gen_ai.provider.name | "Provider" | anthropic / openai / aws.bedrock / google.vertex |
| gen_ai.data_source.id | "RAG source" | Which corpus/store a retrieval hit |
| Content capture | "Prompt logging" | Opt-in capture of messages; store externally in prod |
| Stability opt-in | "Preview mode" | Env var to pin experimental conventions |

## 进一步的阅读

- Z0
- Z0
- Z0
- Z0