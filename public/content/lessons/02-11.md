# 整体方法

> 一群弱学习者，如果组合得当，就会成为一个强学习者。这不是隐喻。这是一个定理。

**Type:** Build
**Language:** Python
**Prerequisites:** Phase 2, Lesson 10 (Bias-Variance Tradeoff)
**Time:** ~120 minutes

## 学习目标

- 从头开始实现AdaBoost和梯度增强，并解释增强如何依次减少偏差
- 构建一个bagging集合，并演示如何在不增加偏差的情况下平均去相关模型来减少方差
- 比较装袋、提升和堆叠方法针对的误差分量
- 评估集成多样性，并解释为什么更多独立的弱学习器会提高多数投票的准确性

## 这个问题

单一决策树训练起来很快，也很容易解释，但它会过度拟合。单一线性模型在复杂边界上欠拟合。您可以花费数天时间来设计完美的模型架构。或者你可以把一堆不完美的模型结合起来得到一个比其中任何一个都更好的模型。

集成方法正是这样做的。它们是在表格数据上赢得Kaggle竞赛的最可靠的技术，它们为大多数生产ML系统提供动力，并且它们说明了实际中的偏差-方差权衡。套袋减少了方差。增强可以减少偏差。Stacking学习在哪些输入上信任哪些模型。

## 这个概念

### 为什么合奏有效？

假设你有N个独立的分类器，每个分类器的准确率都是0.5。多数投票具有准确性：

```
P(majority correct) = sum over k > N/2 of C(N,k) * p^k * (1-p)^(N-k)
```

对于21个准确率均为60%的分类器，多数投票准确率约为74%。有了101个分类器，这个比例上升到了84%。当模型出现不同的错误时，误差就会被抵消。

关键要求是多样性。如果所有的模型都犯同样的错误，那么把它们结合起来也无济于事。合奏之所以有效，是因为它们通过以下方式产生了不同的模型：

- 不同的训练子集（套袋）
- 不同的特征子集（随机森林）
- 顺序纠错（增强）
- 不同的模型家族（堆叠）

### 装袋（自举聚合）

Bagging通过在训练数据的不同bootstrap样本上训练每个模型来创建多样性。

“‘mermaid
流程图TD
D[训练数据]—> B1[Bootstrap样本1]
D—> B2[Bootstrap Sample 2]
D—> B3[Bootstrap Sample 3]
D—> BN[引导样本N]

b1-- > m1[模型1]
b2-- > m2[模型2]
b3-- > m3[模型3]
BN–> MN[模型n]

M1—> V[平均或多数投票]
M2——>v
M3——>v
Mn——> v

V—> P[最终预测]
```

A bootstrap sample is drawn with replacement from the original data, same size as the original. About 63.2% of unique samples appear in each bootstrap. The remaining 36.8% (out-of-bag samples) provide a free validation set.

Bagging reduces variance without increasing bias much. Each individual tree overfits to its bootstrap sample, but the overfitting is different for each tree, so averaging cancels out the noise.

**Random Forests** are bagging with an extra twist: at each split, only a random subset of features is considered. This forces even more diversity among trees. The typical number of candidate features is `sqrt(n_features)` for classification and `n_features / 3` for regression.

### Boosting (Sequential Error Correction)

Boosting trains models sequentially. Each new model focuses on the examples that previous models got wrong.

```mermaid
flowchart LR
    D[Data with weights] --> M1[Model 1]
    M1 --> E1[Find errors]
    E1 --> W1[Increase weights on errors]
    W1 --> M2[Model 2]
    M2 --> E2[Find errors]
    E2 --> W2[Increase weights on errors]
    W2 --> M3[Model 3]
    M3 --> F[Weighted sum of all models]
```

增强可以减少偏差。到目前为止，每一个新模型都修正了集合的系统误差。最后的预测是所有模型的加权和，其中更好的模型获得更高的权重。

这样做的代价是：如果你跑了太多的回合，提升可能会过度拟合，因为它会不断拟合更难的例子，其中一些可能是噪音。

### 演算法

AdaBoost （Adaptive Boosting）是第一个实用的增强算法。它适用于任何基础学习器，通常是决策树桩（深度1树）。

该算法:

```
1. Initialize sample weights: w_i = 1/N for all i

2. For t = 1 to T:
   a. Train weak learner h_t on weighted data
   b. Compute weighted error:
      err_t = sum(w_i * I(h_t(x_i) != y_i)) / sum(w_i)
   c. Compute model weight:
      alpha_t = 0.5 * ln((1 - err_t) / err_t)
   d. Update sample weights:
      w_i = w_i * exp(-alpha_t * y_i * h_t(x_i))
   e. Normalize weights to sum to 1

3. Final prediction: H(x) = sign(sum(alpha_t * h_t(x)))
```

误差较小的模型得到较高的alpha。错误分类的样本会得到更高的权重，所以下一个模型会关注它们。

### 梯度增加

梯度增强将增强推广到任意损失函数。它不是重新加权样本，而是将每个新模型拟合到当前集合的残差（负损失梯度）中。

```
1. Initialize: F_0(x) = argmin_c sum(L(y_i, c))

2. For t = 1 to T:
   a. Compute pseudo-residuals:
      r_i = -dL(y_i, F_{t-1}(x_i)) / dF_{t-1}(x_i)
   b. Fit a tree h_t to the residuals r_i
   c. Find optimal step size:
      gamma_t = argmin_gamma sum(L(y_i, F_{t-1}(x_i) + gamma * h_t(x_i)))
   d. Update:
      F_t(x) = F_{t-1}(x) + learning_rate * gamma_t * h_t(x)

3. Final prediction: F_T(x)
```

对于误差平方损失，伪残差就是实际残差：每棵树都符合前一个集合的误差。

学习率（收缩）控制每棵树贡献多少。较小的学习率需要更多的树，但泛化效果更好。典型值：0.01 ~ 0.3。

### XGBoost：为什么它主宰表格数据

XGBoost（极限梯度增强）是梯度增强与工程优化，使其快速，准确，耐过拟合：

- **正则化目标：**叶子权重的L1和L2惩罚防止单个树过于自信
- **二阶近似：**使用损失的一阶和二阶导数，给出更好的分割决策
- **稀疏感知拆分：**通过学习每次拆分时丢失数据的最佳方向来本地处理丢失的值
- **列子采样：**像随机森林一样，在每个分裂处采样特征以获得多样性
- **加权分位数草图：**有效地找到分布式数据上连续特征的分裂点
- **缓存感知块结构：**内存布局优化的CPU缓存线

对于表格数据，XGBoost（及其后继产品LightGBM）的性能始终优于神经网络。这种情况短期内不会改变。如果您的数据适合包含行和列的表，请从梯度增强开始。

### 叠加(元学习)

堆叠使用多个基本模型的预测作为元学习器的特征。

“‘mermaid
流程图TD
D[训练数据]—> M1[模型1：随机森林]
D—> M2[模型2:SVM]
D—> M3[模型3:Logistic回归]

M1—> P1[预测1]
M2—> P2[预测2]
M3—> P3[预测3]

META[元学习者]
P2——>元
P3——> meta

META—>f[最终预测]
```

The meta-learner learns which base model to trust for which inputs. If the random forest is better at certain regions and the SVM at others, the meta-learner will learn to route accordingly.

To avoid data leakage, base model predictions must be generated via cross-validation on the training set. You never train base models and generate meta-features on the same data.

### Voting

The simplest ensemble. Just combine predictions directly.

- **Hard voting:** Majority vote on class labels.
- **Soft voting:** Average predicted probabilities, pick the class with highest average probability. Usually better because it uses confidence information.

## Build It

### Step 1: Decision Stump (Base Learner)

The code in `code/ensembles.py` implements everything from scratch. We start with a decision stump: a tree with a single split.

```python
class DecisionStump:
    def __init__(self):
        self.feature_idx = None
        self.threshold = None
        self.polarity = 1
        self.alpha = None

    def fit(self, X, y, weights):
        n_samples, n_features = X.shape
        best_error = float("inf")

        for f in range(n_features):
            thresholds = np.unique(X[:, f])
            for thresh in thresholds:
                for polarity in [1, -1]:
                    pred = np.ones(n_samples)
                    pred[polarity * X[:, f] < polarity * thresh] = -1
                    error = np.sum(weights[pred != y])
                    if error < best_error:
                        best_error = error
                        self.feature_idx = f
                        self.threshold = thresh
                        self.polarity = polarity

    def predict(self, X):
        n = X.shape[0]
        pred = np.ones(n)
        idx = self.polarity * X[:, self.feature_idx] < self.polarity * self.threshold
        pred[idx] = -1
        return pred
```

### 步骤2:AdaBoost从零开始

”“python
类AdaBoostScratch:
Def __init__(self, n_estimators=50)：
自我。N_estimators = N_estimators
自我。树桩= []
自我。alpha = []

def (self， X, y)：
n = X.shape[0]
权重= np。Full （n, 1 / n）

对于_in range(self.n_estimators)：
decision - stump （）
stump.fit（X, y, weights）
pred = stump.predict(X)

Err = np。(权重总和pred != y)
Err = np。剪辑（err, 1e-10, 1 - 1e-10）

Alpha = 0.5 * np.log（(1 - err) / err）
权重*= np。Exp （-alpha * y * pred）
Weights /= Weights .sum（）

树桩。= α
self.stumps.append(残)
self.alphas.append(α)

def predict(self， X)：
total = sum(a * s.predict(X) for a, s in zip(self。阿尔法,self.stumps))
返回np.sign(总)
```

### Step 3: Gradient Boosting from Scratch

```python
class GradientBoostingScratch:
    def __init__(self, n_estimators=100, learning_rate=0.1, max_depth=3):
        self.n_estimators = n_estimators
        self.lr = learning_rate
        self.max_depth = max_depth
        self.trees = []
        self.initial_pred = None

    def fit(self, X, y):
        self.initial_pred = np.mean(y)
        current_pred = np.full(len(y), self.initial_pred)

        for _ in range(self.n_estimators):
            residuals = y - current_pred
            tree = SimpleRegressionTree(max_depth=self.max_depth)
            tree.fit(X, residuals)
            update = tree.predict(X)
            current_pred += self.lr * update
            self.trees.append(tree)

    def predict(self, X):
        pred = np.full(X.shape[0], self.initial_pred)
        for tree in self.trees:
            pred += self.lr * tree.predict(X)
        return pred
```

### 第四步：与sklearn进行比较

代码验证了我们从头开始的实现产生与sklearn相似的准确性

## 使用它

### 何时使用每种方法

| Method | Reduces | Best for | Watch out for |
|--------|---------|----------|---------------|
| Bagging / Random Forest | Variance | Noisy data, many features | Does not help with bias |
| AdaBoost | Bias | Clean data, simple base learners | Sensitive to outliers and noise |
| Gradient Boosting | Bias | Tabular data, competitions | Slow to train, easy to overfit without tuning |
| XGBoost / LightGBM | Both | Production tabular ML | Many hyperparameters |
| Stacking | Both | Getting last 1-2% accuracy | Complex, risk of overfitting meta-learner |
| Voting | Variance | Quick combination of diverse models | Only helps if models are diverse |

### 表格数据的生产堆栈

对于大多数表格预测问题，这是尝试的顺序：

1. **LightGBM或XGBoost**与默认参数
2. 调整n_estimators， learning_rate, max_depth, min_child_weight
3. 如果你需要最后的0.5%，用3-5个不同的模型构建一个堆叠集成
4. 始终使用交叉验证

尽管有持续的研究尝试，但在表格数据上的神经网络几乎总是比梯度增强更差。TabNet、NODE和类似的体系结构偶尔可以匹配，但很少能胜过调优的XGBoost。

## 船这

这一课产生了描述你的数据（大小、特征类型、噪音水平、类别平衡）和你正在解决的问题。该提示遍历决策检查表，推荐一种方法，建议启动超参数，并警告该方法的常见错误。也产生了

## 练习

1. 修改AdaBoost实现以跟踪每轮训练后的准确性。图的精度vs.估计器的数量。什么时候收敛？

2. 通过向回归树中添加随机特征子采样，从头开始实现随机森林。训练100棵树将方差减少与单个树进行比较。

3. 在梯度增强实现中，增加提前停止：在每轮之后跟踪验证丢失，并在连续10轮没有改进时停止。它到底需要多少棵树？

4. 构建具有三个基本模型（逻辑回归、决策树、k近邻）和逻辑回归元学习器的堆叠集成。使用5次交叉验证来生成元特性。单独比较每个基本模型。

5. 在相同的数据集上使用默认参数运行XGBoost。将其准确性与从头开始的梯度增强进行比较。两个时间。速度差有多大？

## 关键术语

| Term | What people say | What it actually means |
|------|----------------|----------------------|
| Bagging | "Train on random subsets" | Bootstrap aggregating: train models on bootstrap samples, average predictions to reduce variance |
| Boosting | "Focus on hard examples" | Train models sequentially, each correcting errors of the ensemble so far, to reduce bias |
| AdaBoost | "Reweight the data" | Boosting via sample weight updates; misclassified points get higher weight for the next learner |
| Gradient boosting | "Fit the residuals" | Boosting via fitting each new model to the negative gradient of the loss function |
| XGBoost | "The Kaggle weapon" | Gradient boosting with regularization, second-order optimization, and systems-level speed tricks |
| Stacking | "Models on top of models" | Use predictions of base models as input features for a meta-learner |
| Random forest | "Many randomized trees" | Bagging with decision trees, adding random feature subsampling at each split for diversity |
| Ensemble diversity | "Make different mistakes" | Models must be uncorrelated in their errors for the ensemble to improve over individuals |
| Out-of-bag error | "Free validation" | Samples not in a bootstrap draw (~36.8%) serve as a validation set without needing a holdout |

## 进一步的阅读

- [Schapire & Freund: Boosting: Foundations and Algorithms]（https://mitpress.mit.edu/9780262526036/）——AdaBoost创作者的书
- [Friedman：贪心函数近似：梯度提升机（2001）]（https://statweb.stanford.edu/~jhf/ftp/trebst.pdf）——原始的梯度提升论文
- [Chen & Guestrin: XGBoost (2016)]（https://arxiv.org/abs/1603.02754）——XGBoost论文
- [Wolpert：堆叠泛化（1992）]（https://www.sciencedirect.com/science/article/abs/pii/S0893608005800231）——原始的堆叠论文
- [scikit-learn集成方法](https://scikit-learn.org/stable/modules/ensemble.html) -实用参考