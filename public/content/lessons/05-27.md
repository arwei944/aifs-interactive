# 法学硕士评估- RAGAS， DeepEval, G-Eval

> 精确匹配和F1错过了语义等价。人工审核无法规模化。法学硕士作为法官是生产答案——有足够的校准来相信这个数字。

**Type:** Build
**Languages:** Python
**Prerequisites:** Phase 5 · 13 (Question Answering), Phase 5 · 14 (Information Retrieval)
**Time:** ~75 minutes

## 这个问题

您的RAG系统回答：“2007年6月29日。”
黄金参考是：“2007年6月29日。”
精确匹配得分为0。F1得分~75%。人类得分为100%。

现在乘以10000个测试用例。再乘以每次对检索器、分块、提示符或模型的更改。您需要一个能够理解意义、在规模上运行成本低廉、不会对回归撒谎、并能显示正确的故障模式的评估器。

2026有三个框架来解决这个问题。

- * *拉格。**检索-增强生成评估。四个RAG指标（忠实度，答案相关性，上下文准确性，上下文召回）与NLI + llm判断后端。研究支持,轻量级的。
- * * DeepEval。**法学硕士Pytest。g评估，任务完成，幻觉，偏差指标。CI / CD-native。
- * * G-Eval。**方法（和DeepEval指标）：法学硕士作为法官，具有思维链，自定义标准，0-1分。

这三家公司都依赖法学硕士作为法官。本课为方法及其周围的信任层建立了直观的认识。

## 这个概念

Z0

** LLM-as-judge。**将静态度量替换为LLM，该LLM对给定标题的输出进行评分。给定返回分数。

为什么有效：法学硕士近似于人类的判断，而成本只是人类的一小部分。gpt - 40 -mini每个得分案例约0.003美元，可以以低于5美元的价格运行1000个样本的回归评估。

为什么它默默地失败了：

1. * *法官的偏见。**评委更喜欢较长的答案，来自他们自己的模范家庭的答案，符合提示风格的答案。
2. JSON解析失败。**错误的JSON→NaN评分→从聚合中静默地排除。RAGAS用户知道这种痛苦。带有try/except +显式故障模式的门。
3. **漂移模型版本。**升级裁判会改变每一个参数。冻结法官模型+版本。

** RAG 4 .**

| Metric | Question | Backend |
|--------|----------|---------|
| Faithfulness | Does each claim in the answer come from the retrieved context? | NLI-based entailment |
| Answer relevance | Does the answer address the question? | Generate hypothetical questions from answer; compare to real question |
| Context precision | Of retrieved chunks, what fraction were relevant? | LLM-judge |
| Context recall | Did retrieval return everything needed? | LLM-judge against gold answer |

** G-Eval。**定义一个自定义标准：“答案是否引用了正确的来源？”该框架会自动展开为思维链评估步骤，然后打分0-1。对于RAGAS没有涵盖的领域特定的质量维度来说是好的。

**校准。**永远不要相信原始的裁判分数，除非你有与人类标签的相关性。运行100个手工标记的例子。情节法官vs人类。计算斯皮尔曼。如果rho < 0.7，您的判断规则需要修改。

## 构建它

### 第一步：忠于NLI （ragas风格）

”“python
从输入导入Callable
从变压器进口管道

Nli =管道(“文本分类”，
模特= " MoritzLaurer / DeBERTa-v3-large-mnli-fever-anli-ling-wanli。”
top_k =假设的)

# Z0
# 例如：llm = lambda p: client.messages。创建(模型= " claude-haiku-4-5 ",…).content[0]。text
LLM = Callable[[str], str]


defatomic_claims (answer: str, llm: llm) -> list[str]：
把这个答案分成简单的事实陈述（每行一个）：
{答案}
＂＂＂
返回llm(提示).splitlines ()


deffaithfulness (answer: str, context: str, llm: llm) -> float：
Claims = atomic_claims（answer, llm）
如果没有索赔：
返回0.0
支持= 0
索赔中的索赔：
Result = nli({"text": context, "text_pair": claim})[0]
限定限定= next（(s for s in result if s["label"] == "entailment"), None）
如果限定和限定["score"] > 0.5：
支持+= 1
返回支持的/ len（claims）
```

Decompose the answer into atomic claims. NLI-check each claim against the retrieved context. Faithfulness = fraction supported.

### Step 2: answer relevance

```python
import numpy as np
from sentence_transformers import SentenceTransformer

# encoder: any model implementing .encode(texts, normalize_embeddings=True) -> ndarray
# e.g., encoder = SentenceTransformer("BAAI/bge-small-en-v1.5")

def answer_relevance(question: str, answer: str, encoder, llm: LLM, n: int = 3) -> float:
    prompt = f"Write {n} questions this answer could be the answer to:\n{answer}"
    generated = [line for line in llm(prompt).splitlines() if line.strip()][:n]
    if not generated:
        return 0.0
    q_emb = np.asarray(encoder.encode([question], normalize_embeddings=True)[0])
    g_embs = np.asarray(encoder.encode(generated, normalize_embeddings=True))
    sims = [float(q_emb @ g_emb) for g_emb in g_embs]
    return sum(sims) / len(sims)
```

如果答案暗示了不同的问题，相关性就会下降。

### 步骤3:G-Eval自定义度量

”“python
从deepeval。导入GEval
从deepeval。test_case导入LLMTestCaseParams， LLMTestCase

metric = GEval(
name = "正确",
标准=“答案应该在事实上准确，并符合预期的输出。”
evaluation_steps = [
“读取预期的输出。”
“读取实际输出。”
“在实际输出中列出事实要求。”
对于每一项要求，标记预期输出支持或不支持。
“返回分数=支持分数”，
]，
(LLMTestCaseParams evaluation_params =。输入,LLMTestCaseParams。ACTUAL_OUTPUT LLMTestCaseParams。EXPECTED_OUTPUT),
）

test = LLMTestCase（input=“第一款iPhone是什么时候发布的？”）
actual_output=“ 2007年6月29日”，
expected_output=“2007年6月29日。”)
metric.measure(测试)
打印(公制。分数,metric.reason)
```

The evaluation steps are the rubric. Explicit steps are more stable than implicit "score 0-1" prompts.

### Step 4: CI gate

```python
import deepeval
from deepeval.metrics import FaithfulnessMetric, ContextualRelevancyMetric


def test_rag_system():
    cases = load_regression_cases()
    faith = FaithfulnessMetric(threshold=0.85)
    rel = ContextualRelevancyMetric(threshold=0.7)
    for case in cases:
        faith.measure(case)
        assert faith.score >= 0.85, f"faithfulness regression on {case.id}"
        rel.measure(case)
        assert rel.score >= 0.7, f"relevancy regression on {case.id}"
```

作为pytest文件发布。在每个PR上运行。在回归上合并块。

### 第五步：从头开始玩eval

参见忠实度（答案声明与上下文的重叠）和相关性（答案令牌与问题令牌的重叠）的仅标准库近似。不生产。显示形状。

## 陷阱

- * *没有校准。**与人类标签相关性为0.3的判断是噪声。在发货前需要进行校准。
- * *自我评价。**使用相同的LLM生成和判断会使分数增加10-20%。给法官换一个模范家庭。
- **两两判断中的位置偏差。**评委更喜欢第一个选项。总是随机排序并同时运行。
- **原始聚合隐藏失败。平均得分0.85往往隐藏5%的灾难性失败。总是检查底部的分位数。
- **不受版本控制的eval集随着时间的推移而漂移，会破坏纵向比较。用每次更改标记数据集。
- * * LLM成本。**在规模上，法官决定了成本。使用符合校准阈值的最便宜的模型。gpt - 40 -mini, Claude Haiku, Mistral-small。

## 使用它

2026年的堆栈：

| Use case | Framework |
|---------|-----------|
| RAG quality monitoring | RAGAS (4 metrics) |
| CI/CD regression gates | DeepEval + pytest |
| Custom domain criteria | G-Eval within DeepEval |
| Online live-traffic monitoring | RAGAS with reference-free mode |
| Human-in-the-loop spot checks | LangSmith or Phoenix with annotation UI |
| Red-teaming / safety eval | Promptfoo + DeepEval |

典型的堆栈：RAGAS用于监控，DeepEval用于CI， G-Eval用于新维度。三个都跑；他们的分歧是有益的。

## 船这

另存为

”“减价
---
名称:eval-architect
描述：设计一个具有校准的判断和CI门的LLM评估计划。
版本:1.0.0
阶段:5
教训:27
标签：[nlp， evaluation, rag]
---

给定一个用例（RAG / agent /生成任务），输出：

1. 指标。忠实度/相关性/上下文精确度/上下文召回率+任何自定义的G-Eval标准。
2. 判断模型。命名模型+版本，成本vs准确性的基本原理。
3. 校准。手工标记的集合尺寸，目标斯皮尔曼人比人类>.7。
4. 数据版本控制。标签策略，变更日志，分层。
5. CI门。每个指标的阈值，回归窗口逻辑，底部分位数警报。

拒绝依赖未经测试的判断，至少有50个人工标记的样本。拒绝自我评价（同一模型产生+评委）。拒绝没有底部10%的报告。标记任何在没有并行基线eval的情况下，judge升级着陆的管道。
```

## Exercises

1. **Easy.** Use RAGAS on 10 RAG examples with known hallucinations. Verify the faithfulness metric catches each one.
2. **Medium.** Hand-label 50 QA answers 0-1 for correctness. Score with G-Eval. Measure Spearman rho between judge and human.
3. **Hard.** Build a pytest CI gate with DeepEval. Intentionally regress the retriever. Verify the gate fails. Add bottom-quantile alerting via threshold check on the lowest 10%.

## Key Terms

| Term | What people say | What it actually means |
|------|-----------------|-----------------------|
| LLM-as-judge | Scoring with an LLM | Prompt a judge model to score outputs 0-1 given a rubric. |
| RAGAS | The RAG metric library | Open-source eval framework with 4 reference-free RAG metrics. |
| Faithfulness | Is the answer grounded? | Fraction of answer claims entailed by retrieved context. |
| Context precision | Were retrieved chunks relevant? | Fraction of top-K chunks that actually mattered. |
| Context recall | Did retrieval find everything? | Fraction of gold-answer claims supported by retrieved chunks. |
| G-Eval | Custom LLM judge | Rubric + chain-of-thought eval steps + 0-1 score. |
| Calibration | Trust but verify | Spearman correlation between judge score and human score. |

## Further Reading

- [Es et al. (2023). RAGAS: Automated Evaluation of Retrieval Augmented Generation](https://arxiv.org/abs/2309.15217) — the RAGAS paper.
- [Liu et al. (2023). G-Eval: NLG Evaluation using GPT-4 with Better Human Alignment](https://arxiv.org/abs/2303.16634) — the G-Eval paper.
- [DeepEval docs](https://deepeval.com/docs/metrics-introduction) — open production stack.
- [Zheng et al. (2023). Judging LLM-as-a-Judge with MT-Bench and Chatbot Arena](https://arxiv.org/abs/2306.05685) — biases, calibration, limits.
- [MLflow GenAI Scorer](https://mlflow.org/blog/third-party-scorers) — unifying framework that integrates RAGAS, DeepEval, Phoenix.