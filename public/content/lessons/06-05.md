# Whisper - Architecture and Fine-tuning

> Whisper is a 30-second window converter encoder-decoder that has been trained on 680k hours of multilingual weakly supervised audio-text pairs. One architecture, multiple tasks, supporting 99 languages. Refer to ASR in 2026.

**Type:** Build
**Languages:** Python
**Prerequisites:** Phase 6 · 04 (ASR), Phase 5 · 10 (Attention), Phase 7 · 05 (Full Transformer)
**Time:** ~75 minutes

## This question

In September 2022, OpenAI released Whisper, the first ASR model to be sold as a commodity: paste audio, obtain text, 99 languages, noise-resistant, and run on laptops. By 2024, OpenAI has launched the Large-v3 and Turbo versions. By 2026, Whisper will become the default benchmark for all features, from podcast transcription to voice assistants and YouTube captions.

But Whisper is not a pipe that you can always use as a black box. Domain shift killed it - technical terms, the speaker's accent, proper nouns, short clips, silence. You need to know

1. What exactly is inside?
2. How to give it block, stream, or long-format audio correctly.
3. When to fine-tune and how to fine-tune.

## This concept


*"Architecture. Standard transformer encoder-decoder.

- Input: 30-second logarithmic MEL spectrum, 80 MEL, 10-millisecond jump →3000 frames. Shorter clips are zero-fill, while longer clips are blocks.
- Encoder: convo-downsample (stride 2) + for Large-v3: 32 layers, 1280 dark, 20 heads.
- Decoder: The same size as the encoder.
- Output: 51,865 BPE tokens on the token vocabulary.

Large-v3 has a parameter of 1.55B. Turbo uses a 4-layer decoder (starting from layer 32), reducing latency by 8 times and having a WER hit rate of less than 1%.

** Prompt format. Whisper is a multitasking model controlled by a special token in the decoder prompt:

```
<|startoftranscript|><|en|><|transcribe|><|notimestamps|> Hello world.<|endoftext|>
```

-  forced translation vs. transcription behavior.
- 
- 

Prompts can enable a model to perform many tasks. Change

*A 30-second window. Everything is limited to within 30 seconds. Longer segments need to be divided into blocks; The shorter clips are filled. Windows is not native streaming - that's why WhisperX, whisperstreaming and faster-whisper exist.

*Log-mel normalization. You must use Whisper's preprocessing.

### The 2026 variant

| Variant | Params | Latency (A100) | WER (LibriSpeech-clean) |
|---------|--------|----------------|------------------------|
| Tiny | 39M | 1× realtime | 5.4% |
| Base | 74M | 1× | 4.1% |
| Small | 244M | 1× | 3.0% |
| Medium | 769M | 1× | 2.7% |
| Large-v3 | 1.55B | 2× | 1.8% |
| Large-v3-turbo | 809M | 8× | 1.58% |
| Whisper-Streaming (2024) | 1.55B | streaming | 2.0% |

### Fine-tuning

The standard workflow for 2026

1. Collect 10 to 100 hours of target domain audio and aligned transcripts.
2. "Run
3. Parameter efficiency: Enable LoRA
4. If your time is less than 10 hours, please freeze the encoder. Only adjust the decoder.
5. Use Whisper's own tokener and prompt format; Never swap tokens.

Community result: Fine-tuned the Medium for 20 hours of medical dictation, reducing the WER of medical vocabulary from 12% to 4.5%. After fine-tuning the turbocharger for 4 hours, Iceland's power dropped from 18% to 6%.

## Build it

### Step 1: Run Whisper out of the box

”“python
Import Whisper
Model = whisper.load_model ("large-v3-turbo")
Result = model. transcript (
“clip.wav”,
Language = "en"
Task = "Transcription"
Temperature = 0.0
condition_on_previous_text=False, # Prevent out-of-control repetition
)
print(Result (" text "))
For seg in result["segments"] :
Print (f[{Seg (" start "):.2f} -{Seg [" end "]:.2f} {Seg (" text "]} ")
```

Key defaults you should always override: `temperature=0.0` (sampling defaults to 0.0 → 0.2 → 0.4 … fallback chain), `condition_on_previous_text=False` (prevents the cascading hallucination problem), and `no_speech_threshold=0.6` (silence detection).

### Step 2: chunked long-form

```python
# whisperx is the 2026 reference for long-form with word-level timestamps
import whisperx
model = whisperx.load_model("large-v3-turbo", device="cuda", compute_type="float16")
segments = model.transcribe("1hour.mp3", batch_size=16, chunk_size=30)
```

WhisperX has added (1)Silero VAD gate control, (2) word-level alignment through wav2vec 2.0, and (3) the main force for production transcription in 2026.

### Step 3: Fine-tune using LoRA

”“python
Import from transformer WhisperForConditionalGeneration WhisperProcessor
Import LoraConfig and get_peft_model from peft

model = WhisperForConditionalGeneration.from_pretrained（"openai/whisper-large-v3-turbo"）
lora = LoraConfig（）
R =16, lora_alpha=32, target_modules=["q_proj", "v_proj"]，
lora_dropout=0.1, bias="none", task_type="SEQ_2_SEQ_LM"，
）
Model = get_peft_model（Model, lora）
# Model. print_trainable_parameters() -> ~3M trainable / 809M total
```

Then standard Trainer loop. Checkpoint every 1000 steps. Evaluate with WER on held-out.

### Step 4: inspect what each layer learns

```python
# Grab cross-attention weights during decode to see what the decoder attends to.
with torch.inference_mode():
    out = model.generate(
        input_features=features,
        return_dict_in_generate=True,
        output_attentions=True,
    )
# out.cross_attentions: layer × head × step × src_len
```

Visualization heat map - When the decoder step scans the encoder frame, you will see diagonal alignment. That diagonal line is the concept of Whisper's word timestamp.

## Use it

Stack for 2026

| Situation | Pick |
|-----------|------|
| General English, offline | Large-v3-turbo via `whisperx` |
| Mobile / edge | Whisper-Tiny quantized (int8) or Moonshine |
| Multilingual long-form | Large-v3 via `whisperx` + diarization |
| Low-resource language | Fine-tune Medium or Turbo with LoRA |
| Streaming (2 s latency) | Whisper-Streaming or Parakeet-TDT |
| Word-level timestamps | WhisperX (forced alignment via wav2vec 2.0) |


## These traps will still occur by 2026

- A text of illusion in silence. The subtitles trained by Whisper include "Thank you for watching!" "Subscribe!" And the lyrics. VAD-gate is always performed before the call.
- **** an illusion contaminated the subsequent window. "Collection"
- Short film filling. A 2-second clip filled to 30 seconds can create an illusion in the trailing silence. "Use
- Incorrect mel statistics. Using librosa's smelting instead of Whisper's to produce nearly random output. "Use

## This ship

Save as design a Whisper fine-tuning or inference pipeline for the given domain.

## Practice

1. Easy. Running it marks a whisper-style prompt, calculates the shape budget for decoding, and prints a block schedule for a 10-minute clip.
2. Medium. Try installing
3. "Difficult. Use high frequency

## Key terms

| Term | What people say | What it actually means |
|------|-----------------|-----------------------|
| 30-sec window | Whisper's limit | Hard input cap; chunk longer audio. |
| SOT | Start-of-transcript | `<|startoftranscript|>` kicks off the decoder prompt. |
| Timestamps token | Temporal alignment | Every 0.02 s offset is a special token in the 51k vocab. |
| Turbo | The fast variant | 4-decoder layers, 8× faster, <1% WER regression. |
| WhisperX | The long-form wrapper | VAD + Whisper + wav2vec alignment + diarization. |
| LoRA fine-tune | Efficient tuning | Add low-rank adapters to attention; train ~0.3% of params. |
| Hallucination | The silent failure | Whisper produces fluent English from noise/silence. |

## Further reading

- 
- 
- 
- 
- 