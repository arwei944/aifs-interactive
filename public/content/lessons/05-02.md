# 词袋，TF-IDF，和文本表示

> 先数，再想。到2026年，TF-IDF在def 明确的任务上仍然胜过嵌入。

**Type:** Build
**Languages:** Python
**Prerequisites:** Phase 5 · 01 (Text Processing), Phase 2 · 02 (Linear Regression from Scratch)
**Time:** ~75 minutes

## 这个问题

模型需要数字。你有弦。

每个NLP管道都必须回答同样的问题。如何将可变长度的令牌流转换为分类器可以使用的固定大小的向量？田野找到的第一个答案是最愚蠢的一个。数单词。做一个向量。

该向量比任何嵌入模型都承载了更多的产出NLP。垃圾邮件过滤器，主题分类器，日志异常检测，搜索排名（BM25之前），第一波情感分析，第一个十年学术NLP基准。2026年，从业者仍然首先在狭窄的分类任务中使用它。它快速、可解释，并且在单词存在很重要的任务中，通常与400个参数的嵌入模型没有区别。

这节课从头开始构建单词包，然后是TF-IDF。然后显示scikit-learn在三行中做同样的事情。然后列出让你找到嵌入的失败模式。

## 这个概念

** word Bag (BoW)**抛弃了秩序。对于每个文档，计算每个词汇表单词出现的次数。向量长度是词汇表的大小。位置

**TF-IDF**重新加权BoW。在每个文档中出现的单词是没有信息的，所以要缩小它。一个在语料库中少见但在单个文档中频繁出现的单词是signal，所以要放大它。

```
TF-IDF(w, d) = TF(w, d) * IDF(w)
             = count(w in d) / |d| * log(N / df(w))
```

在哪里The

关键属性：两者都产生具有可解释轴的稀疏向量。您可以查看经过训练的分类器的权重，并读取哪些单词将文档推送到每个类。对于768维BERT嵌入，您无法做到这一点。

## 构建它

### 第一步：建立词汇表

”“python
def build_vocab(文档):
词汇= {}
对于文档中的文档：
对于doc中的token：
如果符号不在词汇中：
Vocab [token] = len（Vocab）
返回词汇
```

Input: list of tokenized documents (any word-level tokenizer will do; the `code/main.py` in this lesson uses a simplified lowercase variant). Output: `{word: index}` dict. Stable insertion order means word index 0 is the first word seen in the first document. Convention varies; scikit-learn sorts alphabetically.

### Step 2: bag of words

```python
def bag_of_words(docs, vocab):
    matrix = [[0] * len(vocab) for _ in docs]
    for i, doc in enumerate(docs):
        for token in doc:
            if token in vocab:
                matrix[i][vocab[token]] += 1
    return matrix
```

”“python
>> >文档=[[“猫”,“坐”,“在”,“垫”],[“猫”、“猫”、“跑”]]
>>> vocab = build_vocab（文档）
>bag_of_words（docs, vocab）
[[1, 1, 1, 1, 0], [2, 0, 0, 0, 1]
```

Rows are documents. Columns are vocabulary indices. Entry `[i][j]` is "how many times word `j` appears in document `i`." Doc 1 has `cat` twice because it did. Doc 0 has `ran` zero times because it did not.

### Step 3: term frequency and document frequency

```python
import math


def term_frequency(doc_bow, doc_length):
    return [c / doc_length if doc_length else 0 for c in doc_bow]


def document_frequency(bow_matrix):
    df = [0] * len(bow_matrix[0])
    for row in bow_matrix:
        for j, count in enumerate(row):
            if count > 0:
                df[j] += 1
    return df


def inverse_document_frequency(df, n_docs):
    return [math.log((n_docs + 1) / (d + 1)) + 1 for d in df]
```

两个值得命名的平滑技巧。The尾迹其他实现使用raw两个工作;平滑的版本更友好。

### 第四步：TF-IDF

”“python
def tfidf (bow_matrix):
N_docs = len（bow_matrix）
Df = document_frequency（bow_matrix）
Idf = inverse_document_frequency（df, n_docs）
Out = []
对于bow_matrix中的行：
长度= sum（行）
Tf = term_frequency（行，长度）
出去了。Append （[tf_j * idf_j for tf_j, idf_j in zip(tf, idf)]）
返回了
```

```python
>>> docs = [
...     ["the", "cat", "sat"],
...     ["the", "dog", "sat"],
...     ["the", "cat", "ran"],
... ]
>>> vocab = build_vocab(docs)
>>> bow = bag_of_words(docs, vocab)
>>> tfidf(bow)
```

三个文件，五个词汇(`the` appears in all three, so its IDF is low. `dog` appears in one, so its IDF is high. 这些向量是稀疏的（大多数条目都很小），而且有区别性的词。

### 步骤5:l2规范化行

”“python
def l2_normalize(矩阵):
Out = []
对于矩阵中的行：
规范=数学。平方根（sum(x * x for x in row)）
出去了。Append （[x / norm if norm else 0 for x in row]）
返回了
```

Without normalization, a longer document gets a larger vector and dominates similarity scores. L2 normalization puts every document on the unit hypersphere. Cosine similarity between rows is now just a dot product.

## Use It

scikit-learn ships the production version.

```python
from sklearn.feature_extraction.text import CountVectorizer, TfidfVectorizer

docs = ["the cat sat on the mat", "the dog sat on the mat", "the cat ran"]

bow_vectorizer = CountVectorizer()
bow = bow_vectorizer.fit_transform(docs)
print(bow_vectorizer.get_feature_names_out())
print(bow.toarray())

tfidf_vectorizer = TfidfVectorizer()
tfidf = tfidf_vectorizer.fit_transform(docs)
print(tfidf.toarray().round(3))
```

`TfidfVectorizer` adds IDF weighting and L2 normalization. 两者都返回稀疏矩阵。对于100k的文档，密集版本不适合内存；保持稀疏，直到分类器需要密集。

改变一切的旋钮：

| Arg | Effect |
|-----|--------|
| `ngram_range=(1, 2)` | Include bigrams. Usually boosts classification. |
| `min_df=2` | Drop words in fewer than 2 docs. Trims vocabulary on noisy data. |
| `max_df=0.95` | Drop words in more than 95% of docs. Approximates stopword removal without a hardcoded list. |
| `stop_words="english"` | scikit-learn's builtin stopword list. Task-dependent — sentiment analysis should *not* drop negations. |
| `sublinear_tf=True` | Use `1 + log(tf)` instead of raw `tf`. Helps when a term repeats many times in one doc. |

### 当TF-IDF仍然获胜时（截至2026年）

- 垃圾邮件检测，主题标注，日志异常标记。文字的存在才是最重要的；语义上的细微差别则不然。
- 低数据体系（数百个标记示例）。TF-IDF加逻辑回归没有预训练成本。
- 延迟很重要。TF-IDF加上一个线性模型在微秒内得到答案。通过转换器嵌入文档需要10-100ms。
- 系统必须解释它们的预测。检查分类器的系数。最重要的积极词汇就是原因。

### 当TF-IDF失败时

语义盲失败。考虑这两个文件：

- “这部电影一点也不好。”
- “这部电影太棒了。”

一种是负面评价。一个是正面的。它们的TF-IDF重叠正好词袋分类器必须记住这个词它可以在足够的数据上学习这一点，但永远不会像理解语法的模型那样优雅。

另一个缺点是：推理时词汇不足。在IMDb评论上训练的BoW模型不知道该怎么做子词嵌入（第04课）处理这个问题。TF-IDF不能。

### 混合：TF-IDF加权嵌入

中等数据分类的2026语用默认值：使用TF-IDF权重作为对词嵌入的关注。

”“python
deftfidf_weighted_embedding (doc, tfidf_scores, embedding_table, dim)：
Vec = [0.0] * dim
Total_weight = 0.0
对于doc中的token：
如果token不在embedding_table中或token不在tfidf_scores中：
继续
Weight = tfidf_scores[token]
Emb = embedding_table[token]
对于范围（dim）中的I：
Vec [i] += weight * emb[i]
Total_weight += weight
如果total_weight == 0：
返回矢量
返回[v / total_weight for v in vec]
```

You get semantic capacity from embeddings, and rare-word emphasis from TF-IDF. Classifier trains on the pooled vector. This outperforms either on its own for sentiment, topic, and intent classification below about 50k labeled examples.

## Ship It

Save as `outputs/prompt-vectorization-picker.md`:

```markdown
---
name: vectorization-picker
description: Given a text-classification task, recommend BoW, TF-IDF, embeddings, or a hybrid.
phase: 5
lesson: 02
---

You recommend a text-vectorization strategy. Given a task description, output:

1. Representation (BoW, TF-IDF, transformer embeddings, or a hybrid). Explain why in one sentence.
2. Specific vectorizer configuration. Name the library. Quote the arguments (`ngram_range`, `min_df`, `max_df`, `sublinear_tf`, `stop_words`).
3. One failure mode to test before shipping.

Refuse to recommend embeddings when the user has under 500 labeled examples unless they show evidence of semantic failure in a TF-IDF baseline. Refuse to remove stopwords for sentiment analysis (negations carry signal). Flag class imbalance as needing more than a vectorizer change.

Example input: "Classifying 30k customer support tickets into 12 categories. Most tickets are 2-3 sentences. English only. Need explainability for audit logs."

Example output:

- Representation: TF-IDF. 30k examples is not small; explainability requirement rules out dense embeddings.
- Config: `TfidfVectorizer(ngram_range=(1, 2), min_df=3, max_df=0.95, sublinear_tf=True, stop_words=None)`. Keep stopwords because category keywords sometimes are stopwords ("not working" vs "working").
- Failure to test: verify `min_df=3` does not drop rare category keywords. Run `get_feature_names_out` filtered by class and eyeball.
```

## 练习

1. **容易。**实施验证相同的文档得分为1.0，不一致的词汇表文档得分为0.0。
2. **介质。**添加参数测试一下
3. **难。**使用GloVe 100d向量构建上述tf - idf加权嵌入混合（下载一次，缓存）。比较20个新闻组数据集上纯TF-IDF和纯均值池嵌入的分类精度。报告哪个赢了哪里。

## 关键术语

| Term | What people say | What it actually means |
|------|-----------------|-----------------------|
| BoW | Word frequency vector | Counts of vocabulary words in one document. Throws away order. |
| TF | Term frequency | Count of a word in a document, optionally normalized by document length. |
| DF | Document frequency | Count of documents containing the word at least once. |
| IDF | Inverse document frequency | `log(N / df)` smoothed. Downweights words that appear everywhere. |
| Sparse vector | Mostly zeros | Vocabulary is typically 10k-100k words; most are absent from any given document. |
| Cosine similarity | Vector angle | Dot product of L2-normalized vectors. 1 is identical, 0 is orthogonal. |

## 进一步的阅读

- 
- 
- 