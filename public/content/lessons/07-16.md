# 推测解码-起草，验证，重复

> 自回归解码是串行的。每个令牌都等待前一个令牌。推测性解码破坏了链：便宜的模型起草N个令牌，昂贵的模型在一次转发中验证所有N个令牌。当草案正确时，你为N代人支付了一个大前锋。

**Type:** Build
**Languages:** Python
**Prerequisites:** Phase 7 · 07 (GPT Causal LM), Phase 7 · 12 (KV Cache & Flash Attention)
**Time:** ~60 minutes

## 这个问题

在H100上，一个70B LLM采样一个令牌需要约30毫秒。一个3B草图模型需要约3毫秒。如果我们让3B提前起草5个令牌，然后运行70B *一次*来验证所有5个，总数是这就是完整的推测解码：用少量额外的GPU内存（草案模型）换取2 - 4倍的解码延迟。

诀窍在于保持分布。由Leviathan等人（2023）和Chen等人同时引入的推测抽样保证了输出序列与大模型自己产生的序列是相同分布的。没有质量权衡。就快。

四类草案-验证者对主导着2026推理：

1. **香草投机（利维坦2023）。**单独的草案模型（如Llama 31b） +验证者（如Llama 370b）。
2. **美杜莎（蔡2024）。**验证器上的多个解码头预测位置没有单独的草稿模型。
3. **EAGLE家族（李2024、2025）。**轻量级草案，重用验证者的隐藏状态；比香草更接近录取率；3 - 4×典型。
4. **前向解码（傅2024）。**雅可比迭代；根本不需要草稿模型。Self-speculation。利基但不依赖。

2026年的每个生产推理堆栈默认都是推测解码。vLLM、TensorRT-LLM、SGLang和llama.cpp都至少支持vanilla + EAGLE-2。

## 这个概念

### 核心算法

给定一个验证者

1. 让
2. **草案**：使用
3. **并行验证**：运行
4. **从左到右接受/拒绝每个草案令牌**：每个
5. 在位置上第一次拒绝之后的所有草稿
6. 接受一切

残差分布技巧是保持输出精确分布的数学洞察力

### 什么决定加速

让让每一步:

- 幼稚代对每个令牌进行1次大模型调用。
- 投机的每个人都叫1个大款

典型的经验法则汇票费用便宜5倍。总挂钟下降~2.5倍。

**α取决于：**

- 草稿接近验证者的程度。相同的家庭/相同的训练数据显著提高α。
- 解码策略。贪婪草案对贪婪验证者：高α。温度采样：更难匹配；接受滴。
- 任务类型。代码和结构化输出接受更多（可预测）；自由形式的创意写作接受的较少。

### 美杜莎-草稿没有草稿模型

Medusa在验证器上用额外的输出头替换了草稿模型。在位置

```
shared trunk → hidden h_t
    ├── head_0: predict token at t+1  (standard LM head)
    ├── head_1: predict token at t+2
    ├── head_2: predict token at t+3
    ├── head_3: predict token at t+4
```

每个头输出自己的逻辑。在推断时，您从每个头部采样以获得候选序列，然后使用一次考虑所有候选延续的树-注意力方案进行一次前向传递验证。

优点：没有第二款。缺点：增加可训练参数；需要一个有监督的微调阶段（约1B个代币）；接受率略低于香草投机与良好的草案。

### EAGLE -通过重用隐藏状态来更好地起草草案

EAGLE-1/2/3 （Li et al., 2024-2025）使草案模型成为一个微小的Transformer（通常是1层），它摄取验证者的最后一层隐藏状态。因为草案看到了验证者的特征表示，它的预测与验证者的输出分布密切相关。录取率从~0.6（香草）上升到0.85+。

EAGLE-3（2025）在候选延续上增加了树搜索。vLLM和SGLang将EAGLE-2/3作为Llama 3/4和Qwen 3的默认规范路径。

### KV缓存舞

验证馈送这将验证器的KV缓存扩展为如果某些草稿被拒绝，则必须将缓存回滚到可接受的前缀长度。

生产实现（vLLM）先写，接受后承诺。这在概念上并不难，但很棘手。

## 构建它

参见我们实现了核心推测抽样算法（拒绝步长+残差分布）：

- 一个“大模型”，它是手工编码分布的确定性软最大值（所以我们可以分析地验证接受数学）。
- 一个“草稿模型”是对大模型的扰动。
- 产生与直接抽样相同的边际分布的接受/拒绝回路。

### 第一步：拒绝步骤

”“python
Def accept_or_reject(q_prob, p_prob, draft_token, u)：
Ratio = q_prob / p_prob if p_prob > 0 else float（"inf"）
返回u < min（1.0, ratio）
```

`u` is a uniform random number. `q_prob` is the verifier's probability for the drafted token. `p_prob` is the draft model's probability. The Leviathan theorem is that this Bernoulli decision, followed by sampling from the residual on rejection, preserves the verifier's distribution exactly.

### Step 2: residual distribution

```python
def residual_dist(q, p):
    raw = [max(0.0, qi - pi) for qi, pi in zip(q, p)]
    s = sum(raw)
    return [r / s for r in raw]
```

减去从任何拒绝中取样。

### 第三步：一个推测的步骤

”“python
def spec_step(prefix, q_model, p_model， N, rng)：
草稿= []
P_probs = []
CTX =列表（前缀）
for _ in range(N)：
P_dist = p_model（ctx）
D = sample（p_dist, rng）
drafts.append (d)
p_probs.append (p_dist [d])
ctx.append (d)

q_dists = [q_model(prefix + drafts[:i]) for i in range(N + 1)]

对于枚举（草稿）中的i， d：
U = rng.random（）
Q_prob = q_dists[i][d]
P_prob = p_pros [i]
如果u < min(1.0， q_prob / p_prob如果p_prob > 0 else float("inf"))：
前缀=前缀+ [d]
其他:
Res = residual_dist（q_dists[i], p_model(prefix)）
前缀=前缀+[样本（res, rng）]
返回前缀
prefix = prefix + [sample(q_dists[N], rng)]
返回前缀
```

Five accepted → one bonus → six tokens produced in one verifier pass.

### Step 4: measure acceptance rate

Run 10,000 speculative steps at varying draft-quality levels. Plot acceptance rate vs. KL divergence between draft and verifier distributions. You should see a clean monotone relationship.

### Step 5: verify distribution equivalence

Empirically: the histogram of tokens produced by the speculative loop should match the histogram produced by sampling directly from the verifier. This is the Leviathan theorem in practice. A chi-square test confirms within sampling error.

## Use It

Production:

```bash
# vLLM with EAGLE
vllm serve meta-llama/Llama-3.1-70B-Instruct \
    --speculative-model /models/llama-3.1-eagle-70b \
    --speculative-draft-tensor-parallel-size 1 \
    --num-speculative-tokens 5

# vLLM with vanilla draft model
vllm serve meta-llama/Llama-3.1-70B-Instruct \
    --speculative-model meta-llama/Llama-3.2-1B-Instruct \
    --num-speculative-tokens 5
```

截至2026年中期，TensorRT-LLM拥有最快的美杜莎路径。`faster-whisper` wraps speculative decoding for Whisper-large with a small draft.

*挑选草稿：**

| Strategy | When to pick | Speedup |
|----------|--------------|---------|
| Vanilla draft (1B/3B Llama family) | Fast prototype, no training | 1.8–2.3× |
| Medusa heads | You can fine-tune the verifier | 2–3× |
| EAGLE-2 / 3 | Production, max speed | 3–4× |
| Lookahead | No draft, no training, no extra params | 1.3–1.6× |

**当不指定解码：**

- 单序列生成1-5个令牌。开销占主导地位。
- 极具创意/高温取样（α下降）。
- 内存受限的部署（草案模型增加了VRAM）。

## 船这

参见该技能为新的推理工作负载选择推测解码策略（vanilla / Medusa / EAGLE / lookahead）和调优参数（N, draft temperature）。

## 练习

1. **容易。**运行确认投机令牌分布与验证者在卡方p > 0.05范围内的50,000个令牌的直接样本分布相匹配。
2. **介质。**绘图加速（每个大模型向前代币）作为函数确定最佳方案(提示：每个验证调用的期望令牌=
3. **难。**实现一个微小的美杜莎：从第14课取顶点GPT，增加3个额外的LM头，预测位置t+2， t+3, t+4。用一个关节多头损失来训练小莎士比亚。比较接受率与截断相同模型的香草草案。
4. **难。**实现回滚：从10个令牌前缀KV缓存开始，提供5个草案令牌，在位置3模拟拒绝。在下一次迭代中验证您的缓存读取是否正确匹配“前缀+前2个接受的草稿”。

## 关键术语

| Term | What people say | What it actually means |
|------|-----------------|-----------------------|
| Draft model | "The cheap one" | A smaller model that proposes candidate tokens; usually 10–50× cheaper than the verifier. |
| Verifier | "The big one" | The target model whose distribution we preserve; runs once per speculative step. |
| Acceptance rate (α) | "How often the draft is right" | Per-token probability that the verifier accepts the draft. 0.7–0.9 typical. |
| Residual distribution | "The rejection fallback" | `(q - p)_+` normalized; sampling from this on rejection preserves the verifier's distribution. |
| Bonus token | "The free one" | When all N drafts accepted, sample one more from the verifier's next-step distribution. |
| Medusa | "Draft-less speculative" | Multiple LM heads on the verifier predict positions t+1..t+k in parallel. |
| EAGLE | "Hidden-state draft" | Tiny transformer draft conditioned on the verifier's last-layer hidden states. |
| Lookahead decoding | "Jacobi iteration" | Self-speculation using a fixed-point iteration; no draft model. |
| Tree attention | "Verify many candidates at once" | Branching verification that considers several draft continuations simultaneously. |
| KV rollback | "Undo rejected drafts" | Scratch KV buffer; commit on acceptance, discard on reject. |

## 进一步的阅读

- 
- Z0干净的伯努利拒绝证明。
- Z0tree-attention验证。
- Z0hidden-state-conditioned草案。
- Z0动态树深度。
- 
- 
- 
- 