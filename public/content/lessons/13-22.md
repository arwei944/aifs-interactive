# 技能和代理sdk -人类技能，代理。md, OpenAI Apps SDK

> MCP说的是“存在什么工具”。技能指的是“如何完成一项任务”。2026将两层都堆叠起来。Anthropic的代理技能（开放标准，2025年12月）作为技能发布。循序渐进的信息披露。OpenAI的Apps SDK是MCP + widget元数据。代理。Md（现在在60,000多个回购中）作为项目级代理上下文位于回购根。本课命名每个涵盖和建立一个最低的技能。md + AGENTS。在代理间传输的数据包。

**Type:** Learn
**Languages:** Python (stdlib, SKILL.md parser and loader)
**Prerequisites:** Phase 13 · 07 (MCP server)
**Time:** ~45 minutes

## 学习目标

- 区分三个层次：agent。md（项目背景），技能。md（可重用的技术诀窍），MCP（工具）。
- 写一项技能。md与YAML frontmatter和渐进式披露。
- 将skills文件系统样式加载到代理运行时中。
- 用MCP服务器和agent组成一个技能。所以一个包可以在Claude Code， Cursor和Codex中工作。

## 这个问题

工程师将编写发布说明的工作流程提炼成一个多步骤提示：“阅读最新合并的pr。按区域分组。总结每个。按照团队的风格写一个变更日志。发布到Slack草稿。”他们把它放进了他们团队的概念文档中。

现在他们想要使用来自Claude Code、Cursor和Codex CLI的工作流。每个代理都有不同的方式加载指令：Claude Code斜杠命令，光标规则，Codex工程师复制工作流程三次，并维护三个副本。

代理。md和SKILL。让我们一起解决这个问题：

- * *代理。Md **位于回购根目录。每个兼容的代理都在会话开始时读取它。“这个项目是如何运作的？”惯例是什么？哪些命令运行测试？”
- * *技巧。md**是一个可移植的bundle: YAML frontmatter (name, description) + markdown body +可选资源。支持技能的代理根据需要按名称加载技能。
- **MCP**（阶段13·06-14）处理技能需要调用的工具。

三层，一个便携的神器。

## 这个概念

### AGENTS.md (agents.md)

2025年底推出，到2026年4月将被6万多家回购机构采用。在repo根目录下有一个文件。格式:

”“减价
# 项目:我的服务

## 约定
- 严格模式的TypeScript。
- 在Python端使用Pydantic模型。
- 下面运行测试

## 构建并运行
- Z0
- Z0
```

Agents read this on session start and use it to calibrate their behavior for that project. Every coding agent in 2026 supports AGENTS.md: Claude Code, Cursor, Codex, Copilot Workspace, opencode, Windsurf, Zed.

### SKILL.md format

Anthropic's Agent Skills (released as an open standard December 2025):

```markdown
---
name: release-notes-writer
description: Write a changelog entry for the latest merged PRs following this project's style.
---

# Release notes writer

When invoked, run these steps:

1. List PRs merged since the last tag. Use `gh pr list --base main --state merged`.
2. Group by label: feature, fix, chore, docs.
3. For each PR in each group, write one line: `- <title> (#<num>)`.
4. Draft the release notes and stage them in CHANGELOG.md.

If the user says "ship", run `git tag vX.Y.Z` and `gh release create`.

## Notes

- Never include commits without a PR.
- Skip "chore" entries from the public changelog.
```

Frontmatter声明技能的身份。主体是技能加载时显示给模型的提示。

### 进步的信息披露

技能可以引用代理只在需要时获取的子资源。例子:

```
skills/
  release-notes-writer/
    SKILL.md
    style-guide.md
    template.md
    scripts/
      generate.sh
```

技能。Md说“请参阅风格指南”。我喜欢时尚规则。”代理提取样式指南。只有当技能是主动运行。这避免了用模型可能不需要的细节来填充提示符。

### 文件系统发现

代理运行时扫描已知的SKILL目录。md文件:

- Z0
- 项目
- Z0

加载是按文件夹名称和标题Claude Code、Anthropic Claude Agent SDK和SkillKit（跨代理）都遵循这种模式。

### Anthropic Claude Agent SDK

Z0当用户调用某个技能时，代理循环将其分派给该技能。

### OpenAI Apps SDK

2025年10月发射；直接建立在MCP上。将OpenAI先前的连接器和自定义GPT操作统一在一个开发人员界面下。一个Apps SDK应用是：

- MCP服务器（工具、资源、提示）。
- 加上ChatGPT的UI小部件元数据。
- 加上一个可选的MCP应用程序

相同的协议，更丰富的用户体验。

### 通过SkillKit的跨代理可移植性

像SkillKit这样的工具和类似的跨代理分发层可以翻译单个技能。md转换为32+ AI代理（Claude Code, Cursor, Codex, Gemini CLI， OpenCode等）的原生格式。真理的源泉；许多消费者。

### 三层堆栈

| Layer | File | Loaded when | Purpose |
|-------|------|-------------|---------|
| AGENTS.md | repo root | session start | project-level conventions |
| SKILL.md | skills directory | skill invoked | reusable workflow |
| MCP server | external process | tools needed | callable actions |

这三个组成：代理读取AGENTS。在会话开始时，用户调用一个技能，该技能的指令包括MCP工具调用，座席通过MCP客户端调度。

## 使用它

Z0Md解析器和加载器。它发现了然后模拟调用的代理循环

看点：

- 使用最小的标准库解析器解析YAML前文
- 技能体逐字存储；代理在调用时将其添加到系统提示符中。
- 通过a演示渐进披露

## 船这

这一课产生了给定一个工作流，该技能产生组合的技能。md + AGENTS。md + MCP-server-blueprint包，可跨代理移植。

## 练习

1. 运行在下面添加第二个技能

2. 编写agent。本课程回购的Md。包括测试命令、样式约定和第13阶段心智模型。

3. 将团队内部文档中的多步骤工作流移植到SKILL.md中。验证它在Claude Code中加载。

4. 手动将技能转换为光标和Codex的本地规则格式。计算格式之间的差异-这是SkillKit自动化的翻译表面。

5. 阅读Anthropic Agent Skills博客文章。找出Claude Agent SDK中本课的加载程序没有涵盖的一个特性。（提示：代理子调用。）

## 关键术语

| Term | What people say | What it actually means |
|------|----------------|------------------------|
| SKILL.md | "The skill file" | YAML frontmatter plus markdown body, loaded by agent runtime |
| AGENTS.md | "Repo-root agent context" | Project-level conventions file read on session start |
| Progressive disclosure | "Lazy-load sub-resources" | Skill body references files pulled only when needed |
| Frontmatter | "YAML block at top" | Metadata (name, description) in `---` delimiters |
| Claude Agent SDK | "Anthropic's skill runtime" | `@anthropic-ai/claude-agent-sdk`, loads skills and routes |
| OpenAI Apps SDK | "MCP + widget meta" | OpenAI's dev surface built on MCP plus ChatGPT UI hooks |
| Skill discovery | "Filesystem scan" | Walk known dirs for SKILL.md, key by name |
| Cross-agent portability | "One skill many agents" | Translate one SKILL.md to 32+ agents via SkillKit-style tools |
| Agent Skill | "Portable know-how" | Reusable task template outside MCP's tool concept |
| Apps SDK | "MCP plus ChatGPT UI" | Connectors and Custom GPTs unified on MCP |

## 进一步的阅读

- Z0
- Z0Md格式参考
- Z0
- Z0Md格式和采用清单
- Z0